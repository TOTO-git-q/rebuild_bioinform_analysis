# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml

# Audit/extraction scratch (the original bundle is read-only reference, not committed code)
_extract/

# Local run output (projects are created at runtime under any path the user picks)
/runs/
*.tmp
