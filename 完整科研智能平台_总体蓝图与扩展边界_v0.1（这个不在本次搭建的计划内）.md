# 完整科研智能平台：总体蓝图与扩展边界

- **文档状态**：平台级产品蓝图
- **版本**：v0.1
- **日期**：2026-06-23
- **用途**：指导自动生信核心完成后的平台扩充、模块拆分和长期开发
- **核心依赖**：自动生信系统核心闭环

---

## 1. 平台定义

完整科研智能平台不是一个大型 Agent，也不是把所有功能塞进自动生信系统。

它是围绕实验室科研活动构建的一组协同软件，其中：

```text
自动生信系统
= 从科研问题到经审核的计算证据和复现包

完整科研智能平台
= 自动生信系统
+ 用户工作台
+ 项目与权限管理
+ 计算与存储基础设施
+ 实验室知识库
+ 学生复现与教学
+ 报告协作
+ 湿实验和后续验证接口
+ 安全、审计和运维
```

平台的最终目标是让一个课题从问题提出、计算分析、证据审核、学生复现、知识沉淀到后续实验形成可管理的生命周期，同时保持各模块职责清晰。

---

## 2. 平台用户

### 2.1 课题负责人

希望完成：

- 用自然语言提出研究问题；
- 查看系统如何理解和拆解问题；
- 批准数据集、方法和高风险执行；
- 阅读证据、限制和冲突；
- 导出任务给学生复现；
- 判断项目是否值得继续；
- 在不维护复杂环境的情况下查看所有结果。

### 2.2 生信人员

希望完成：

- 查看详细数据、参数、日志和 QC；
- 修改或新增 MethodContract；
- 调试失败任务；
- 审核 Engineering Worker 补丁；
- 比较不同方法；
- 管理可复用工作流。

### 2.3 学生

希望完成：

- 接收明确的复现任务；
- 获取输入、环境、步骤和预期输出；
- 提交复现结果；
- 查看与原始结果的差异；
- 根据错误定位需要学习的知识。

### 2.4 实验室管理员

希望完成：

- 管理账号、权限和项目成员；
- 管理存储、计算资源和配额；
- 管理备份和保留策略；
- 检查敏感数据和外部共享；
- 维护服务健康状态。

### 2.5 合作者或审稿人

希望完成：

- 在受限权限下查看方法和证据；
- 下载可公开的复现材料；
- 对结果提交评论或审核意见；
- 验证图表与结果表之间的追溯关系。

---

## 3. 平台总体架构

```text
┌────────────────────────────────────────────────────────────┐
│ 1. 用户入口与科研工作台                                   │
│ Web / Desktop / Chat / Project Workspace / Approval UI      │
└──────────────────────────────┬─────────────────────────────┘
                               │
┌──────────────────────────────▼─────────────────────────────┐
│ 2. 项目、身份、权限与协作                                  │
│ Users / Roles / Projects / Tasks / Notifications / Sharing  │
└──────────────────────────────┬─────────────────────────────┘
                               │
┌──────────────────────────────▼─────────────────────────────┐
│ 3. 自动生信核心                                            │
│ ResearchSpec → Workflow → QC → Evidence → Claim → Bundle    │
└───────────────┬───────────────────────────┬────────────────┘
                │                           │
┌───────────────▼──────────────┐ ┌──────────▼─────────────────┐
│ 4. Tool/MCP Gateway          │ │ 5. 任务执行与计算基础设施 │
│ DB / Literature / Annotation │ │ Workers / Containers / HPC │
│ Codex / Evidence services    │ │ Queues / Storage / Logging │
└───────────────┬──────────────┘ └──────────┬─────────────────┘
                │                           │
┌───────────────▼───────────────────────────▼────────────────┐
│ 6. 数据、Artifact、Evidence 与实验室知识层                 │
│ Object Store / Registry / Evidence DB / Knowledge Graph     │
└───────────────┬───────────────────────────┬────────────────┘
                │                           │
┌───────────────▼──────────────┐ ┌──────────▼─────────────────┐
│ 7. 复现、教学与交接         │ │ 8. 报告、论文与展示       │
│ Student Tasks / Comparison   │ │ Reports / Figures / Export │
└───────────────┬──────────────┘ └──────────┬─────────────────┘
                │                           │
┌───────────────▼───────────────────────────▼────────────────┐
│ 9. 湿实验、ELN、样本与验证系统接口                          │
└────────────────────────────────────────────────────────────┘

横切能力：Security / Audit / Policy / Monitoring / Backup
```

---

## 4. 模块一：用户入口与科研工作台

### 4.1 职责

为不同用户提供简单、稳定的访问方式，不承载生信核心逻辑。

### 4.2 功能

- 自然语言问题输入；
- 文件上传；
- ResearchSpec 和歧义确认；
- 项目状态时间线；
- 数据集和方法批准；
- 任务 DAG 可视化；
- 运行日志和错误查看；
- QC、Evidence 和 Claim 浏览；
- 报告阅读；
- 复现包导出；
- 学生复现结果比较；
- 人工审核和签字。

### 4.3 Windows XP 老人家模式

平台应提供一个极简访问形态：

- 浏览器或薄客户端；
- 不要求本地安装 Python、R、Docker、Conda 或 Nextflow；
- 默认只显示项目状态、关键结论、风险和审批按钮；
- 技术细节可展开查看；
- 计算全部在服务器或工作站执行；
- 报告和复现包可一键下载；
- 历史项目打开后不依赖当前软件版本重新计算才能阅读。

### 4.4 不应承担

- 科学状态机；
- 数据库检索逻辑；
- 统计计算；
- Evidence 判定；
- Worker 权限管理的底层实现。

---

## 5. 模块二：身份、权限、项目和协作

### 5.1 身份与角色

建议支持：

```text
Principal Investigator
Bioinformatician
Researcher
Student
Reviewer
Collaborator
Administrator
Service Account
```

### 5.2 权限维度

- 查看项目；
- 上传数据；
- 运行任务；
- 批准数据集；
- 批准方法；
- 批准代码变更；
- 审核 Claim；
- 导出数据或复现包；
- 邀请外部协作者；
- 查看敏感信息；
- 删除或归档项目。

### 5.3 项目管理

- 项目创建、归档和恢复；
- 成员和角色；
- 项目标签；
- 里程碑；
- 人工任务；
- 通知；
- 审批请求；
- 项目讨论；
- 外部共享链接；
- 数据保留策略。

### 5.4 与自动生信系统的边界

项目平台负责“谁能做什么”，自动生信系统负责“科学流程下一步是什么”。

---

## 6. 模块三：自动生信系统核心

该模块由独立文档《自动生信系统核心闭环：详细需求规格》定义。

它负责：

```text
问题规范化
→ 子问题
→ 证据计划
→ 数据核验
→ 方法选择
→ DAG
→ 执行
→ QC
→ Evidence
→ Claim
→ Alignment Audit
→ 报告数据
→ 复现包
```

平台其他模块不得绕过自动生信核心直接把未审核结果写入正式 Evidence 或 Claim。

---

## 7. 模块四：Tool/MCP Gateway

### 7.1 定位

MCP 或等价工具网关是 Agent、外部服务和受控工具之间的统一边界。

### 7.2 工具类别

#### 数据资源工具

```text
search_public_datasets
get_dataset_metadata
fetch_dataset_files
verify_dataset_accession
query_cellxgene
query_sra
```

#### 文献工具

```text
search_literature
get_article_metadata
extract_paper_methods
verify_pmid_doi
link_paper_to_dataset
```

#### 注释工具

```text
query_gene_ontology
query_pathways
query_surfaceome
query_secretome
query_protein_localization
map_orthologs
```

#### 方法工具

```text
list_method_contracts
check_method_compatibility
compile_workflow
validate_task_packet
```

#### 执行工具

```text
submit_analysis_task
submit_engineering_task
get_task_status
collect_artifacts
cancel_task
```

#### Evidence 工具

```text
write_evidence_item
query_evidence
write_claim
trace_claim
validate_claim_ceiling
```

### 7.3 网关要求

- 认证和授权；
- 输入输出 schema；
- 超时；
- 限流；
- 重试策略；
- 审计日志；
- 幂等性；
- 工具版本；
- 数据来源记录；
- 敏感数据策略；
- 测试环境与生产环境隔离。

---

## 8. 模块五：任务执行与计算基础设施

### 8.1 职责

提供可靠的计算资源，不决定科学问题和结论。

### 8.2 组成

- 任务队列；
- Worker 注册；
- 容器运行时；
- 本地工作站执行；
- HPC 调度；
- 云任务执行；
- CPU/GPU/内存配额；
- Worker 心跳；
- 任务 lease；
- 超时和重试；
- 网络策略；
- 临时空间清理；
- 运行日志；
- 资源计量。

### 8.3 Worker 类型

```text
Data Download Worker
R Analysis Worker
Python Analysis Worker
Nextflow Worker
Codex Engineering Worker
QC Worker
Report Rendering Worker
Reproduction Verification Worker
```

### 8.4 Codex Engineering Worker

必须：

- 在隔离 worktree 或容器工作；
- 只接受 EngineeringTaskPacket；
- 限制修改路径；
- 输出 patch、测试和日志；
- 不能直接合并主分支；
- 不能直接修改 Evidence 和 Claim；
- 在达到最大修复次数后停止并升级人工。

---

## 9. 模块六：数据、Artifact 和 Evidence 层

### 9.1 对象存储

保存：

- 原始下载文件；
- 中间矩阵；
- 结果表；
- 图形；
- 环境包；
- 日志；
- 复现包；
- 报告附件。

### 9.2 Artifact Registry

管理：

- 唯一 ID；
- 校验值；
- 生产任务；
- 输入 lineage；
- schema；
- 状态；
- 敏感等级；
- 生命周期和保留策略。

### 9.3 Evidence DB

保存：

- 数据集；
- 论文；
- 方法；
- TaskRun；
- QCReport；
- EvidenceItem；
- Claim；
- 审核意见；
- 追溯关系。

### 9.4 实验室知识库

这是 Evidence DB 的上层或并列系统，用于跨项目检索：

- 实验室分析过哪些数据集；
- 某候选在哪些项目出现；
- 哪些方法经过实验室验证；
- 常见失败模式；
- 哪些结论已有独立复现；
- 哪些项目使用了过期数据库或方法；
- 人员离开后保留的项目经验。

### 9.5 Memory 与 Evidence 的边界

```text
对话 Memory
= 用户偏好、会话上下文和暂时工作记忆

Evidence DB
= 可审计的科学证据和正式运行记录

Knowledge Base
= 跨项目沉淀的实验室知识
```

对话记忆不能替代 Evidence DB。

---

## 10. 模块七：学生复现、教学和交接

### 10.1 复现任务管理

- 从自动生信系统导入 ReproductionBundle；
- 指派学生；
- 设置截止时间；
- 记录计算环境；
- 提交结果；
- 自动比较原始结果；
- 保存差异解释；
- 导师审核和签字。

### 10.2 复现结果状态

```text
NOT_STARTED
IN_PROGRESS
BLOCKED
SUBMITTED
MATCHED
PARTIALLY_MATCHED
NOT_REPRODUCED
REQUIRES_REVIEW
ACCEPTED
REJECTED
```

### 10.3 教学能力

平台可根据任务自动生成：

- 背景知识；
- 必须理解的统计概念；
- 最小练习；
- 关键文献；
- 常见错误；
- 阶段性检查问题；
- 复现失败的诊断建议。

### 10.4 边界

教学系统可以解释方法，但不得改变自动生信系统的正式结果和 Evidence。

---

## 11. 模块八：报告、展示和科研写作接口

### 11.1 输入约束

报告系统只接受：

- 已审核 Claim；
- EvidenceItem；
- QC 摘要；
- DatasetManifest；
- MethodPlan；
- QuestionAlignmentReport；
- 已登记图表和表格。

### 11.2 输出

- 负责人摘要；
- 技术报告；
- 组会版材料；
- 合作者报告；
- 方法学附录；
- 图表索引；
- 论文材料草稿；
- 对外共享包。

### 11.3 原则

- 不隐藏负结果；
- 不允许图表脱离源表；
- 不允许报告生成器创造新 Claim；
- 不允许省略关键限制；
- 每个结论可点击追溯到 Evidence。

---

## 12. 模块九：湿实验、ELN 和后续验证接口

自动生信系统可能提出：

```text
候选需要蛋白水平验证
候选需要流式或免疫染色
候选需要功能干预实验
```

完整平台可与下列系统集成：

- ELN；
- LIMS；
- 样本库；
- 试剂库存；
- 仪器预约；
- 动物实验管理；
- 临床样本管理；
- 实验结果采集。

### 12.1 回流机制

湿实验结果可以作为新的 EvidenceItem 回流，但必须：

- 有实验方案和样本信息；
- 有原始数据或可验证记录；
- 有 QC；
- 明确实验类型和证据等级；
- 不覆盖原有计算证据，而是形成新的支持或反对证据。

---

## 13. 横切能力：安全、合规与审计

### 13.1 数据分类

```text
PUBLIC
INTERNAL
CONFIDENTIAL
CLINICAL_SENSITIVE
RESTRICTED_EXTERNAL_SHARING
```

### 13.2 策略

- 项目级网络政策；
- 外部模型调用政策；
- 数据导出政策；
- 访问日志；
- API 密钥管理；
- 数据脱敏；
- 复现包敏感信息扫描；
- 数据保留和销毁；
- 合作者访问到期；
- 代码变更审计；
- 人工批准记录。

### 13.3 不可变审计

至少记录：

```text
谁在什么时候做了什么
调用了什么工具
读取和写入了哪些对象
批准或驳回了什么
修改了什么代码或参数
生成了什么 Evidence 或 Claim
导出了哪些数据
```

---

## 14. 横切能力：监控、备份和运维

### 14.1 监控

- 服务健康；
- 队列长度；
- Worker 状态；
- 任务失败率；
- 数据库响应；
- 存储使用；
- 成本；
- LLM 调用量；
- 工具错误率；
- 复现成功率；
- 审批等待时间。

### 14.2 备份

需要分别备份：

- 项目元数据；
- Evidence DB；
- Artifact Registry；
- 对象存储；
- schema 和 MethodContract；
- 事件日志；
- 审批记录；
- 复现包。

### 14.3 历史可读性

即使新版本方法或界面上线，历史项目必须仍然能够：

- 查看；
- 追溯；
- 下载；
- 在原环境定义下复现；
- 明确标识已过时的方法或数据库版本。

---

## 15. 模块职责矩阵

| 能力 | 自动生信核心 | 完整平台 | 外部基础设施 |
|---|---:|---:|---:|
| 自然语言问题规范化 | 主责 | 提供界面 |  |
| 数据发现和可行性判断 | 主责 | 展示/审批 | 外部数据库 |
| 方法选择和 Workflow DAG | 主责 | 展示/审批 |  |
| 分析执行控制 | 主责 | 状态展示 | 计算平台执行 |
| QC、Evidence、Claim | 主责 | 浏览/签字 |  |
| 复现包生成 | 主责 | 分发/管理 | 存储 |
| 学生任务分配 | 接口 | 主责 |  |
| 学生评分和教学 | 提供技术信息 | 主责 |  |
| 用户与权限 | 遵守策略 | 主责 | 身份服务可承载 |
| 任务队列和容器 | 提交与监控 | 管理视图 | 主责 |
| 实验室知识库 | 提供结构化数据 | 主责 | 搜索引擎可承载 |
| 报告协作和 PPT | 提供事实材料 | 主责 | 文档工具可承载 |
| 湿实验管理 | 提出验证需求 | 集成 | ELN/LIMS 主责 |
| 备份与灾难恢复 | 提供对象清单 | 管理策略 | 主责 |

---

## 16. 平台数据域

建议将平台数据划分为：

```text
Identity Domain
Project Domain
Research Question Domain
Workflow Domain
Execution Domain
Artifact Domain
QC Domain
Evidence Domain
Claim Domain
Reproduction Domain
Teaching Domain
Wet-lab Domain
Audit Domain
```

不同域可以共享 ID，但不得通过直接修改彼此数据库形成隐式耦合。优先通过 API 和事件交换。

---

## 17. 平台事件

平台级事件示例：

```text
ProjectCreated
ResearchQuestionSubmitted
ResearchSpecNeedsConfirmation
DatasetApprovalRequested
DatasetApproved
MethodApprovalRequested
WorkflowCompiled
TaskStarted
TaskFailed
QCFailed
EvidenceAccepted
ClaimNeedsReview
FinalReportReady
ReproductionBundleCreated
ReproductionAssigned
ReproductionSubmitted
ReproductionMismatchDetected
WetLabValidationRequested
ProjectArchived
```

事件应支持通知、审计和跨模块自动化。

---

## 18. 平台集成接口

### 18.1 自动生信核心对外 API

```text
POST /projects/{id}/research-requests
GET  /projects/{id}/state
GET  /projects/{id}/workflow
POST /approvals/{id}/decision
GET  /tasks/{id}
GET  /artifacts/{id}
GET  /evidence/{id}
GET  /claims/{id}
POST /projects/{id}/reproduction-bundles
POST /reproductions/{id}/compare
```

### 18.2 身份和项目接口

```text
get_user_identity
get_project_role
check_permission
get_project_policy
publish_approval_request
publish_notification
```

### 18.3 计算接口

```text
submit_task
cancel_task
get_task_status
get_worker_health
collect_logs
collect_artifacts
```

### 18.4 知识库接口

```text
index_project_evidence
search_datasets_across_projects
search_candidates_across_projects
find_prior_failures
find_reproduction_status
```

---

## 19. 建议代码与部署边界

可采用单仓库多服务，也可逐步拆分。概念目录如下：

```text
research-platform/
├─ apps/
│  ├─ web-workbench/
│  ├─ desktop-thin-client/
│  └─ admin-console/
├─ services/
│  ├─ identity-project-service/
│  ├─ bioinfo-core/
│  ├─ tool-mcp-gateway/
│  ├─ task-execution-service/
│  ├─ artifact-service/
│  ├─ evidence-service/
│  ├─ knowledge-service/
│  ├─ reproduction-service/
│  ├─ teaching-service/
│  ├─ reporting-service/
│  └─ notification-service/
├─ workers/
│  ├─ r-worker/
│  ├─ python-worker/
│  ├─ nextflow-worker/
│  ├─ codex-worker/
│  ├─ qc-worker/
│  └─ reproduction-worker/
├─ packages/
│  ├─ schemas/
│  ├─ client-sdk/
│  ├─ method-contracts/
│  ├─ policy-models/
│  └─ event-models/
├─ infrastructure/
│  ├─ containers/
│  ├─ database/
│  ├─ object-storage/
│  ├─ queues/
│  ├─ monitoring/
│  └─ deployment/
└─ docs/
```

第一阶段不必立即拆成很多微服务。职责和数据模型先分清，再根据规模拆分部署。

---

## 20. 平台扩展顺序

## 阶段 P0：自动生信核心独立跑通

目标：

```text
自然语言问题
→ 经审核 Evidence/Claim
→ 复现包
```

此时只需要简单 CLI 或最小 Web 页面。

## 阶段 P1：科研工作台

增加：

- 项目列表；
- 问题输入；
- 状态时间线；
- 审批界面；
- 报告和 Artifact 浏览；
- 一键导出复现包。

## 阶段 P2：用户、权限和多项目

增加：

- 实验室成员；
- 角色权限；
- 项目共享；
- 外部协作者；
- 数据策略；
- 通知。

## 阶段 P3：学生复现和教学

增加：

- 复现任务分配；
- 提交；
- 自动比较；
- 导师审核；
- 学习材料。

## 阶段 P4：实验室知识库

增加跨项目：

- 数据集检索；
- 候选检索；
- 方法经验；
- 失败案例；
- 复现记录；
- 结论更新提醒。

## 阶段 P5：正式计算和存储基础设施

增加：

- 多 Worker；
- HPC/云；
- 对象存储；
- 资源配额；
- 监控和成本；
- 备份和恢复。

## 阶段 P6：湿实验和 ELN/LIMS 集成

把计算候选、验证请求和实验结果接入统一证据链。

## 阶段 P7：多实验室或机构级平台

增加：

- 租户隔离；
- 机构权限；
- 合规审计；
- 跨机构共享；
- 数据出域审批；
- 高可用部署。

---

## 21. 平台级非功能要求

### 21.1 易用性

- 非技术用户可以完成问题提交、审批、报告查看和复现包导出；
- 技术细节按需展开；
- 错误提示说明发生了什么、影响什么、下一步是什么。

### 21.2 可靠性

- 状态可恢复；
- 任务可重试；
- Worker 中断不丢失记录；
- 核心 Evidence 和事件不可因 UI 崩溃丢失。

### 21.3 可扩展性

- 可增加新数据库工具；
- 可增加新 MethodContract；
- 可增加新 Worker；
- 可增加新报告和教学模块；
- 不要求修改所有模块才能接入一种新组学。

### 21.4 可审计性

- 所有关键决策、运行和导出都有记录；
- 所有正式结论可追溯；
- 人工审批可追踪。

### 21.5 可移植性

- 历史项目不依赖单台电脑；
- 计算环境可容器化或明确描述；
- 数据和元数据可导出；
- 不被某一聊天平台或单一 LLM 锁定。

### 21.6 成本控制

- LLM 和 Codex 调用有预算；
- 重试有上限；
- 大数据下载和计算需预估；
- 可查看项目资源消耗。

---

## 22. 完整平台验收方向

完整平台成熟后，应能够完成以下场景：

### 场景一：负责人发起任务

负责人用自然语言提出问题，确认范围，批准数据集和方法，最后查看证据等级和限制。

### 场景二：生信人员审核

生信人员查看 TaskRun、参数和 QC，修复一个格式适配问题，审核 Codex 补丁并重跑局部子图。

### 场景三：学生独立复现

负责人导出复现包并指派学生。学生在另一个环境运行，平台比较结果并标记差异。

### 场景四：跨项目知识检索

负责人查询某候选在实验室多个项目中的支持、反对证据和复现状态。

### 场景五：计算结果进入湿实验

系统生成验证建议，实验结果回流为新的 EvidenceItem，并更新 Claim 等级。

### 场景六：旧电脑查看

负责人无需安装分析环境，在简单界面中查看历史项目、审批和下载复现包。

---

## 23. 防止平台边界失控的规则

1. 新功能若直接决定生信问题、方法、QC、Evidence 或 Claim，应进入自动生信核心；
2. 新功能若主要管理人员、任务、协作或展示，应进入平台层；
3. 新功能若主要提供计算、存储、队列或容器，应进入基础设施层；
4. 新功能若主要管理实验步骤、样本和试剂，应进入 ELN/LIMS 层；
5. 对话 Memory 不得替代正式数据库；
6. UI 不得绕过核心状态机直接修改 Evidence；
7. 报告系统不得从未经审核的结果生成正式结论；
8. Codex 不得成为不受约束的通用科研决策者；
9. 平台不得以“自动化率”为唯一指标；
10. 复现率、追溯完整度、错误拦截率和结论越级率应作为核心质量指标。

---

## 24. 平台长期指标

建议跟踪：

```text
端到端任务完成率
数据集核验成功率
方法兼容性拦截率
QC 失败发现率
Claim 越级拦截率
人工审核修改率
第三方复现成功率
复现差异定位时间
跨项目组件复用率
任务平均等待和执行时间
计算与 LLM 成本
历史项目可读率
敏感数据违规导出次数
```

这些指标比“有多少 Agent”更能反映平台价值。

---

## 25. 最终边界总结

### 自动生信系统

```text
负责科学计算闭环：
问题 → 数据 → 方法 → 执行 → QC → Evidence → Claim → 复现包
```

### 完整平台

```text
负责把自动生信系统变成实验室可长期使用的软件：
用户 → 项目 → 权限 → 审批 → 协作 → 教学 → 知识 → 运维 → 湿实验接口
```

### 外部基础设施

```text
负责提供通用能力：
身份、容器、集群、云、对象存储、备份、监控、通知和第三方数据库
```

最重要的架构原则是：

> 自动生信核心必须能够脱离某个具体工作台独立运行；完整平台则通过稳定 API 和事件把它组织成实验室可监督、可交接、可复现的科研生产系统。

---

## 26. 参考资料

本蓝图综合了当前项目讨论、手绘流程、`PilotDeck_bioinfo_agent_architecture_summary.md` 中关于操作舱、MCP、Codex、Evidence DB 与 Human Review 的分层，以及 `dev_handoff_for_codex_cn.md` 中关于 TargetCompass 现有模块和后续应用化方向的说明。
