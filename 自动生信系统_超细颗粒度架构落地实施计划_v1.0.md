# 自动生信系统核心闭环：超细颗粒度架构落地实施计划

- **文档性质**：从产品需求基线转换得到的可执行架构与实施基线
- **计划版本**：v1.0-draft
- **生成日期**：2026-06-23
- **输入基线**：
  - `自动生信系统核心闭环_详细需求规格_v0.1(1).md`
  - `Codex_粗颗粒度需求转化与用户确认门_Skill草案.md`
- **执行状态**：`NEEDS_CONFIRMATION`
- **适用对象**：产品负责人、系统架构师、生信方法负责人、后端工程师、平台工程师、测试与审计人员、Codex 类工程执行单元

---

## 快速导航

1. 执行纪律与确认基线：第 0～1 节
2. 目标架构、横切契约与领域模型：第 2～5 节
3. 状态机、API、Agent、Worker 与测试：第 6～9 节
4. Release 路线与 28 个工作包：第 10～11 节
5. 20 项验收映射、波次、风险和变更控制：第 12～15 节
6. Codex 工作包模板与用户检查清单：第 16～17 节
7. 最终确认和完成判定：第 18～19 节

> 实际启动时只先执行 `WP-00`。在现状审计和 D-01～D-06 未冻结前，不进入批量开发。

---

## 0. 文档用途与执行纪律

本文件不是概念架构说明，而是可以逐项领取、实现、测试和验收的实施计划。任何执行者不得把某个大章节作为单个开发任务直接实施；必须按本文的 `WP-*` 工作包和 `T-*` 叶子任务执行。

### 0.1 叶子任务最低字段

每个叶子任务至少明确：

```yaml
task_id: 唯一任务编号
purpose: 该任务解决的具体问题
inputs: 已存在且可核验的输入
preconditions: 开始前必须成立的条件
method: 具体实施方法
method_rationale: 为什么采用该方法
write_scope: 允许修改的目录、模块或对象
outputs: 必须产生的文件、Schema、迁移、接口或记录
validation: 可自动或人工核验的证据
failure_conditions: 必须判定失败或停止的条件
audit_evidence: 日志、diff、测试报告、checksum 或审批记录
rollback_or_recovery: 失败后的恢复方式
approval_gate: 是否需要人工批准
```

### 0.2 Codex 执行约束

1. 每次只执行一个工作包，或一个工作包中依赖连续且写入范围一致的少量叶子任务。
2. 不允许使用“实现完整模块”“搭建全部系统”“优化整体架构”作为可执行任务标题。
3. 每次执行前必须给出：目标、写入范围、禁止修改范围、输入、输出、测试命令和停止条件。
4. 任何 Schema、数据库迁移、状态迁移、外部 API、真实数据、密钥、执行环境或最终 Claim 的变化都必须形成可审计记录。
5. 未满足当前工作包的退出标准，不得进入下一个依赖工作包。
6. 发现基线假设不成立时，停止受影响任务并进入 `RECONFIRM_REQUIRED`，不得通过临时补丁静默改变架构。

---

## 1. 执行前确认基线

### 1.1 用户希望得到的最终结果

完成一套能够真实运行的自动生信系统架构，使系统可以把自然语言科研问题逐步转换为：

```text
结构化研究规约
→ 可核验子问题与证据计划
→ 真实数据与文献资源
→ 数据可行性和锁定清单
→ 受 MethodContract 约束的任务 DAG
→ 隔离执行与完整运行记录
→ 四层 QC
→ EvidenceItem
→ 证据边界内的 Claim
→ 原问题对照审核
→ 报告与可独立复现包
```

完成标准不是“生成了若干 Agent 或代码文件”，而是从输入到复现包存在一条真实、可追溯、可停止、可回滚、可审计的纵向链路。

### 1.2 已确认事实

- 系统本体不包含实验室人员管理、学生作业管理、通用协作平台、湿实验管理、通用云计算平台和完整可视化工作台。
- 第一版应采用真实纵向切片，不追求任意组学、任意格式和自动因果推断。
- Agent 负责受约束推理；数据库查询、文件核验、统计执行、状态推进、checksum、QC 和审计必须由确定性工具或服务完成。
- 所有关键阶段必须产出结构化对象，且计划、执行和审核逻辑分离。
- 系统必须保留负结果、冲突、失败和无法判断结果，并允许以合法非成功终态结束。

### 1.3 尚未获得仓库审计支持的假设

以下内容是实施默认值，不得伪装成现有系统事实：

- 尚未提供现有源码仓库、数据库、部署环境和历史接口，因此计划同时支持绿地项目与已有仓库改造。
- 尚未确认生产部署目标；默认先完成单机容器化开发环境和可迁移部署接口。
- 尚未确认真实敏感数据策略；默认 MVP 仅使用公开数据，敏感数据能力只建设策略与隔离骨架。
- 尚未确认 LLM、对象存储和计算集群供应商；默认通过 provider/adapter 接口隔离。
- “老化骨骼肌 SASP”仅可作为可选验收场景，不得硬编码为系统预设。

### 1.4 建议默认架构决策

| 决策项 | 建议默认值 | 原因 | 何时重新确认 |
|---|---|---|---|
| 总体形态 | 模块化单体控制面 + 独立 Worker 进程 | 先保证领域边界和事务一致性，避免过早微服务化 | 并发、团队或部署边界证明需要拆分时 |
| 权威状态 | PostgreSQL 中的不可变事件日志 + 查询投影 | 状态可重建、审批和历史不可被静默改写 | 采用外部事件平台且仍能保证同等审计时 |
| Artifact | S3 兼容对象存储；本地开发使用 MinIO 或文件适配器 | 大文件不进入数据库，统一 checksum 与 URI | 组织已有受控存储平台时 |
| 异步投递 | 队列只负责投递；数据库状态为唯一事实源 | 避免把 Broker 状态误当项目状态 | 更换队列实现不改变领域协议 |
| 生信工作流 | Nextflow 适配器作为首个 Workflow Worker | 与容器、HPC/云执行和复现包兼容 | 组织已有 Snakemake/WDL 平台时 |
| Schema | Python 类型模型 + JSON Schema 导出；数据库约束补强 | Agent 与服务共享结构化契约 | 多语言客户端成为主要约束时 |
| Evidence 存储 | 先使用关系数据库和显式关联表 | MVP 可审计、查询和事务边界清晰 | 图关系规模或查询复杂度达到明确瓶颈时 |
| 自动化级别 | A1：ResearchSpec 与 DatasetManifest 需批准，后续按策略自动运行 | 在科研风险与实施效率之间取得保守平衡 | 用户选择 A0/A2/A3 时 |
| 首条执行路线 | 先接通 bulk RNA-seq 执行脊柱，再加入 sc/snRNA donor-level 路线 | 先验证通用执行、Artifact、QC、Evidence 架构 | 若已有成熟 scRNA 管线可直接复用 |

### 1.5 明确非目标

本计划不把以下事项纳入 MVP 完成条件：

- 任意多组学自动融合；
- 自动因果推断和无人工审核的机制结论；
- 自动湿实验设计闭环；
- 任意数据格式自动适配；
- 完整浏览器可视化工作台；
- Kubernetes、多区域高可用和无限水平扩展；
- Engineering Worker 自动合并主分支或绕过代码审核；
- 无人监督发布、投稿或提交科学结论。

### 1.6 执行前必须确认的六项决策

| 编号 | 决策 | 默认方案 | 不确认的影响 |
|---|---|---|---|
| D-01 | 绿地项目还是改造现有仓库 | 先做只读仓库审计；无仓库则绿地 | 目录、迁移和兼容策略无法冻结 |
| D-02 | 首个真实验收课题与公开数据源 | 选择一个公开 GEO 课题，不硬编码疾病 | 无法固定端到端金标准 |
| D-03 | 部署目标 | 本地 Docker Compose；保留 HPC 适配接口 | 环境、存储和调度设计可能变化 |
| D-04 | 自动化等级 | A1 | 人工门禁数量无法固定 |
| D-05 | LLM 与联网策略 | provider-agnostic；仅公共非敏感信息可外发 | Agent 网关与数据策略无法定稿 |
| D-06 | 主工作流技术 | Nextflow 首适配 | Workflow Worker 与复现包结构会变化 |

> 本计划可以先用于只读审计和架构准备；进入正式代码实施前应把上述决策写入 `architecture_baseline.yaml` 并由用户确认。

---

## 2. 架构完成的可观察定义

只有同时满足以下条件，才能声明“系统架构已经落地”：

1. 每个核心领域对象均有版本化 Schema、语义校验器、数据库映射和正反例契约测试。
2. 项目状态只能通过命令与事件推进，无法通过直接修改状态字段绕过门禁。
3. Agent 输出不能直接成为事实、任务或 Claim；必须经过 Schema、语义、政策和审批校验。
4. 外部数据集 ID、论文标识、文件存在性、checksum 和运行状态均由工具核验。
5. DatasetManifest、MethodContract、WorkflowPlan、TaskPacket、TaskRun、Artifact、QC、EvidenceItem 和 Claim 形成完整追溯链。
6. Worker 重试、重复消息或进程中断不会造成重复副作用或状态倒退。
7. 至少一条真实 bulk 路线和一条 donor-level sc/snRNA 路线可在隔离环境中运行。
8. 四层 QC 可对预设错误样例做出正确的 PASS、RETRY、REPLAN、REJECT 或 NEED_HUMAN_REVIEW 决策。
9. Claim 生成器无法把 RNA 证据提升为蛋白分泌或因果结论。
10. QuestionAlignmentReport 能识别物种、组织、比较组、时间范围漂移和证据越级。
11. 报告只消费已审核 EvidenceItem 与 Claim，不直接自由总结原始结果目录。
12. 复现包能在干净环境重跑，并按照 comparison spec 生成可比较结论。
13. 任一最终数值、图、表和结论均可反向追溯到输入文件、代码版本、环境、参数、TaskRun 和 QC。
14. 数据不足或方法不适用时，系统能进入合法终态，而不是伪造完成。

---

## 3. 目标系统总体架构

### 3.1 三平面架构

```text
┌──────────────────────────────────────────────────────────────────────┐
│  交互与控制平面                                                     │
│  API/CLI ─ Approval ─ Project State ─ Orchestrator ─ Agent Gateway   │
└──────────────────────────────┬───────────────────────────────────────┘
                               │ Commands / Events / Typed Objects
┌──────────────────────────────▼───────────────────────────────────────┐
│  数据与执行平面                                                     │
│  Tool Broker ─ Resource Adapters ─ Queue ─ Workers ─ Nextflow       │
│  Sandbox ─ Artifact Store ─ Method Registry ─ Environment Registry  │
└──────────────────────────────┬───────────────────────────────────────┘
                               │ TaskRun / Artifact / QC inputs
┌──────────────────────────────▼───────────────────────────────────────┐
│  证据与审计平面                                                     │
│  QC Engine ─ Evidence Registry ─ Claim Engine ─ Alignment Auditor   │
│  Audit Log ─ Lineage ─ Report Builder ─ Reproduction Builder        │
└──────────────────────────────────────────────────────────────────────┘
```

### 3.2 权威数据源

| 信息 | 唯一事实源 | 禁止替代来源 |
|---|---|---|
| 当前项目状态 | 事件日志重建的状态投影 | LLM 对话、队列状态、单个 JSON 文件 |
| 研究规约 | 已确认版本的 ResearchSpec | Prompt 中的临时文字 |
| 正式数据集与样本 | 锁定的 DatasetManifest 版本 | 搜索结果摘要、临时表格 |
| 方法可用性 | MethodContract + CompatibilityDecision | Agent 口头判断 |
| 实际执行结果 | TaskRun + ArtifactManifest | Worker stdout 摘要 |
| QC 结论 | 版本化 QCReport | 报告生成器的自由判断 |
| 科学证据 | 已准入 EvidenceItem | 未审核结果表或 p 值 |
| 最终科学表述 | 经门禁的 Claim | 报告生成器新写的结论 |
| 审批 | 绑定对象版本的 ApprovalDecision | 聊天中的模糊“可以” |

### 3.3 有界上下文

| 上下文 | 核心职责 | 允许依赖 | 禁止直接承担 |
|---|---|---|---|
| Project & Policy | 项目、原始请求、数据策略、权限和审批 | Audit、Identity adapter | 科学方法选择 |
| Research Design | ResearchSpec、Scope、SubQuestion、EvidencePlan | Agent Gateway、Ontology adapter | 数据下载和统计 |
| Resource Registry | 检索、核验、Dataset/Paper/Annotation Profile | Tool Broker、External adapters | 宣称数据可用性而不核验 |
| Data Governance | Feasibility、DatasetManifest、样本锁定、许可证 | Resource Registry、Policy | 静默改动已锁定数据 |
| Method Registry | MethodContract、环境、实现引用和兼容性 | Data Governance | 执行任务或批准自身方法 |
| Workflow Planning | WorkflowPlan、TaskPacket、DAG 编译 | Method Registry、Artifact specs | 直接运行代码 |
| Execution | 队列、Worker、Sandbox、TaskRun | Workflow Planning、Artifact Store | 形成科学 Claim |
| Artifact & Lineage | Artifact、checksum、输入输出关系 | Execution、Object Store | 解释生物学意义 |
| Quality | Execution/Data/Statistical/Biological QC | Artifact、MethodContract | 修改原始结果以通过 QC |
| Evidence & Claim | EvidenceItem、Claim、反对证据、等级 | Quality、Research Design | 读取未准入结果自由叙事 |
| Alignment | 问题覆盖、范围一致性、越级和遗漏审核 | Claim、ResearchSpec | 产生新证据 |
| Reporting & Reproduction | 报告、索引、复现包和比较结果 | 已审核对象、Artifact | 提高 Claim 等级 |
| Audit & Observability | 事件、工具调用、日志、指标和追踪 | 所有上下文 | 成为业务状态的替代源 |

### 3.4 依赖规则

1. 上层推理模块只能通过应用服务读取领域对象，不得直接扫描任意数据库表。
2. Agent Gateway 不能直接写业务表；它只能返回提案，由命令处理器校验并持久化。
3. Worker 只能读取 TaskPacket 声明的输入和限定目录，不能直接修改 ResearchSpec、DatasetManifest、MethodContract、QC 或 Claim。
4. Report Builder 只能读取已批准 Claim、EvidenceItem、QC 摘要和追溯索引。
5. 队列、Nextflow 和对象存储均为基础设施适配器，替换实现不得改变领域状态机。
6. 任何跨上下文写操作必须由一个应用命令协调，并在同一事务中写事件和 outbox 记录。

### 3.5 推荐部署拓扑

#### MVP 本地拓扑

```text
api            控制面 HTTP API
worker-general 通用任务与外部工具调用
worker-analysis 注册分析模块
worker-review  QC、证据和审核任务
postgres       领域数据、事件、投影、审计索引
redis/broker   异步消息投递，不作为权威状态
minio          Artifact 和复现包
nextflow       由 Workflow Worker 调用
otel-collector 可观测数据收集
```

#### 后续扩展拓扑

- API、Orchestrator、Worker 可独立扩缩容；
- 对象存储可替换为组织 S3/HPC 文件系统适配器；
- Worker executor 可增加 Local、Slurm、Kubernetes、Cloud Batch 适配器；
- PostgreSQL 仍保存领域状态和审计索引，计算大文件保持外置；
- 不因为部署拆分而复制领域规则。

### 3.6 推荐仓库结构

```text
auto-bioinfo/
├─ apps/
│  ├─ api/                         # HTTP 入口与依赖注入
│  ├─ worker/                      # 通用、分析和审核 Worker 入口
│  └─ cli/                         # 与 API 同命令语义的 CLI
├─ src/auto_bioinfo/
│  ├─ domain/
│  │  ├─ project_policy/
│  │  ├─ research_design/
│  │  ├─ resources/
│  │  ├─ data_governance/
│  │  ├─ methods/
│  │  ├─ workflows/
│  │  ├─ execution/
│  │  ├─ artifacts/
│  │  ├─ quality/
│  │  ├─ evidence/
│  │  ├─ alignment/
│  │  └─ reproduction/
│  ├─ application/
│  │  ├─ commands/
│  │  ├─ queries/
│  │  ├─ orchestration/
│  │  └─ approvals/
│  ├─ infrastructure/
│  │  ├─ db/
│  │  ├─ messaging/
│  │  ├─ object_store/
│  │  ├─ llm/
│  │  ├─ tools/
│  │  ├─ workflow_engines/
│  │  ├─ executors/
│  │  └─ observability/
│  └─ interfaces/
│     ├─ http/
│     ├─ cli/
│     └─ events/
├─ schemas/                        # 导出的 JSON Schema 与示例
├─ methods/
│  ├─ contracts/                   # MethodContract
│  ├─ implementations/             # 小型注册分析模块
│  └─ testdata/
├─ workflows/
│  ├─ nextflow/
│  └─ profiles/
├─ prompts/                        # 版本化 Agent prompt 与评测集
├─ policies/                       # 网络、数据、审批、Claim 规则
├─ migrations/
├─ tests/
│  ├─ unit/
│  ├─ contract/
│  ├─ integration/
│  ├─ workflow/
│  ├─ adversarial/
│  └─ e2e/
├─ deploy/
│  ├─ compose/
│  └─ examples/
├─ docs/
│  ├─ adr/
│  ├─ architecture/
│  ├─ runbooks/
│  └─ acceptance/
└─ scripts/
```

### 3.7 必须先冻结的 ADR

| ADR | 决策主题 | 最低内容 |
|---|---|---|
| ADR-001 | 模块化单体与拆分边界 | 为什么不先微服务化；何时允许拆分 |
| ADR-002 | 事件日志和状态投影 | 事件不可变、重建、并发和迁移策略 |
| ADR-003 | Artifact 外置存储 | URI、checksum、命名、保留和删除策略 |
| ADR-004 | 队列非权威 | outbox、幂等、重复消息和 Worker lease |
| ADR-005 | Workflow Engine 适配 | Nextflow 与领域 DAG 的职责边界 |
| ADR-006 | Schema 与版本兼容 | JSON Schema、数据库、API 和事件版本策略 |
| ADR-007 | Agent Gateway | Prompt 版本、结构化输出、工具权限和敏感数据策略 |
| ADR-008 | 科学证据门禁 | QC→Evidence→Claim→Alignment 的不可绕过链 |
| ADR-009 | 数据与网络安全 | sensitivity、网络白名单、密钥和导出扫描 |
| ADR-010 | 复现语义 | 代码、容器、参数、checksum 和比较等级 |

---

## 4. 横切技术契约

### 4.1 所有版本化对象的公共字段

```yaml
id: UUID
project_id: UUID
schema_version: string
object_version: integer
status: enum
created_at: UTC timestamp
created_by: actor reference
supersedes_id: UUID | null
content_hash: sha256
correlation_id: UUID
causation_id: UUID | null
metadata: constrained object
```

规则：

- 已锁定、已批准或已用于下游任务的对象不得原地覆盖；修改必须产生新版本。
- 所有时间使用 UTC；显示层再进行时区转换。
- `content_hash` 对规范化序列化结果计算，确保同内容可比。
- Schema 默认拒绝未知字段；扩展字段必须进入显式 `extensions` 命名空间。
- 枚举值只能追加或版本迁移，不能静默改变旧值语义。

### 4.2 ID 与引用规则

- 领域对象使用稳定 UUID；文件路径、accession、队列 task id 和 LLM message id 均不得替代领域 ID。
- 所有依赖使用 ID 引用；路径只作为 Artifact 属性。
- 外部标识保存 `namespace`、`identifier`、`version`、`verified_at` 和 `verification_source`。
- 每个 Claim 必须显式列出支持与反对 EvidenceItem ID。

### 4.3 幂等与并发

- 所有命令接受 `idempotency_key`；同一项目和命令类型下唯一。
- Aggregate 写入使用 `expected_version` 乐观并发控制。
- Worker 获取任务时建立 lease 和 heartbeat；lease 过期后可重新领取。
- 重复消息只能产生同一结果或被安全忽略，不能重复下载、重复计费或重复创建 Claim。
- 对外工具调用使用调用指纹；可缓存操作不得因重试生成不可识别的第二份事实记录。

### 4.4 事件与 outbox

```yaml
event_id:
aggregate_type:
aggregate_id:
aggregate_version:
event_type:
event_schema_version:
payload:
actor:
correlation_id:
causation_id:
recorded_at:
```

- 业务事件和 outbox 记录在同一数据库事务提交。
- 投递成功只更新 outbox，不修改历史事件。
- 查询投影可以重建；事件日志不可通过 UPDATE 改写。
- 事件 payload 中不存大文件和密钥，只存引用。

### 4.5 统一错误信封

```yaml
error_code:
category:
message:
retryable:
object_id:
task_run_id:
details:
user_action_required:
caused_by:
recorded_at:
```

错误分类必须映射到：

```text
INPUT_FAILURE
RESOURCE_DISCOVERY_FAILURE
DATA_ACCESS_FAILURE
METADATA_FAILURE
COMPATIBILITY_FAILURE
ENVIRONMENT_FAILURE
EXECUTION_FAILURE
QC_FAILURE
STATISTICAL_FAILURE
BIOLOGICAL_INCONSISTENCY
AUDIT_FAILURE
POLICY_FAILURE
```

### 4.6 审批记录

审批必须绑定“对象 ID + 对象版本 + gate 类型”，不能只绑定项目：

```yaml
approval_id:
gate_type:
subject_type:
subject_id:
subject_version:
decision: APPROVE | REJECT | REQUEST_CHANGES
actor:
reason:
conditions:
created_at:
expires_at:
```

旧版本已批准不等于新版本自动批准。

### 4.7 数据敏感等级

最少支持：

```text
PUBLIC
INTERNAL
RESTRICTED
SENSITIVE_BIOMEDICAL
```

每个 Artifact、ProjectPolicy 和外部工具调用都必须携带或推导 sensitivity；高等级数据不得发送到未批准的外部 LLM、外部对象存储或公共日志。

### 4.8 目录与对象存储约定

```text
projects/{project_id}/
  external/{source}/{external_id}/
  runs/{task_run_id}/inputs/
  runs/{task_run_id}/work/
  runs/{task_run_id}/outputs/
  artifacts/{artifact_id}/
  reports/{report_id}/
  reproduction/{bundle_id}/
```

- 数据库保存 URI，不把路径当业务主键。
- Worker 输入只读、工作目录隔离、输出目录限定。
- Artifact 注册成功前不得被下游正式消费。
- 删除采用保留策略和 tombstone；正式证据关联的 Artifact 不允许硬删除。

---
## 5. 领域对象、数据库与追溯关系

### 5.1 对象清单与持久化策略

| 对象 | 所属上下文 | 持久化/版本策略 | 关键关系与约束 |
|---|---|---|---|
| Project | Project & Policy | 可变投影；历史由事件重建 | 一对多 Request、Event、Approval |
| OriginalRequest | Project & Policy | 不可变 | 保存原文、附件引用、提交者，不覆盖 |
| ProjectPolicy | Project & Policy | 版本化 | 网络、数据、自动化等级、导出策略 |
| ProjectEvent | Audit | 仅追加 | 权威状态历史 |
| ApprovalRequest | Approval | 仅追加状态变化 | 必须绑定 subject version |
| ApprovalDecision | Approval | 不可变 | 决定不得原地修改 |
| ResearchSpec | Research Design | 草案可新版本；确认版不可变 | 关联 OriginalRequest 与 Claim ceiling |
| AmbiguityReport | Research Design | 版本化 | 每个歧义记录影响、默认值和状态 |
| ScopeBundle | Research Design | 版本化 | 聚合 OntologyMapping 和警告 |
| OntologyMapping | Research Design | 不可变事实记录 | 原词、标准 ID、来源、置信度、候选 |
| ScopeWarning | Research Design | 仅追加/关闭 | 低置信度或范围冲突 |
| SubQuestion | Research Design | 版本化 | 父子关系、证据类型、Claim 上限 |
| QuestionDependency | Research Design | 版本化边 | 必须无环或明确允许的逻辑依赖 |
| EvidencePlan | Research Design | 版本化 | 每个 SubQuestion 至少一个计划或停止理由 |
| EvidenceGap | Research Design | 仅追加状态变化 | 未满足证据与后续动作 |
| SearchRun | Resource Registry | 不可变 | 工具、查询、过滤、原始响应 Artifact |
| ResourceCandidate | Resource Registry | 版本化核验状态 | 外部标识必须唯一化和去重 |
| DatasetProfile | Resource Registry | 版本化快照 | 基于工具核验，不接受 LLM 单独声明 |
| PaperProfile | Resource Registry | 版本化快照 | PMID/DOI 等唯一标识必须核验 |
| AnnotationSourceProfile | Resource Registry | 版本化快照 | 数据库版本、许可和证据类型 |
| DatasetFeasibilityReport | Data Governance | 不可变评估版本 | 绑定 DatasetProfile、SubQuestion 和规则版本 |
| DatasetManifest | Data Governance | 锁定后不可变 | 变更产生新版本并记录原因 |
| DatasetManifestItem | Data Governance | 随 Manifest 版本化 | 文件、checksum、来源和用途 |
| SampleManifestItem | Data Governance | 随 Manifest 版本化 | subject/donor、分组、排除和协变量 |
| MethodContract | Method Registry | 版本化、签名/哈希 | 实现与科学边界必须同时版本化 |
| CompatibilityDecision | Method Registry | 不可变 | 绑定 MethodContract、DatasetManifest、SubQuestion |
| MethodPlan | Method Registry | 版本化 | 主方法、备选方法、风险和停止条件 |
| WorkflowPlan | Workflow Planning | 编译后不可变 | 绑定输入对象版本和 compiler 版本 |
| TaskPacket | Workflow Planning | 不可变 | 四种 packet 子类型；声明读写范围 |
| TaskDependency | Workflow Planning | 不可变边 | 使用 task/artifact ID，不靠路径推断 |
| ArtifactExpectation | Workflow Planning | 不可变 | 类型、Schema、必需性和生产任务 |
| TaskRun | Execution | 每次运行独立记录 | 一个 TaskPacket 可有多次 Run |
| TaskAttempt | Execution | 仅追加 | 重试、错误、开始结束、lease |
| ToolCallRecord | Execution/Audit | 不可变 | 请求/响应摘要、hash、策略、成本 |
| ArtifactManifest | Artifact | 不可变注册记录 | URI、checksum、producer、lineage |
| ArtifactEdge | Artifact | 不可变边 | input→output、derived-from、used-by |
| QCReport | Quality | 版本化 | 绑定 TaskRun/Artifact/规则版本 |
| QCFinding | Quality | 不可变 | 严重度、范围、证据、建议动作 |
| EvidenceItem | Evidence | 准入后不可变 | 只引用通过门禁的结果；可被撤回但不删除 |
| EvidenceRelation | Evidence | 不可变边 | supports/opposes/replicates/contradicts |
| Claim | Evidence | 版本化 | 明确等级、范围、支持和反对证据 |
| ClaimEvidenceLink | Evidence | 随 Claim 版本化 | 记录关系类型和权重依据 |
| QuestionAlignmentReport | Alignment | 不可变审核版本 | 绑定 ResearchSpec 与 Claim 集合版本 |
| ClaimCorrection | Alignment | 不可变 | 原 Claim、新建议、阻断级别 |
| ReportManifest | Reporting | 不可变 | 输入 Claim/Evidence 版本和输出 Artifact |
| ReproductionBundleManifest | Reproduction | 不可变 | bundle 内容、顺序、环境、预期输出 |
| ReproductionComparison | Reproduction | 不可变 | 结果等级、容差和差异说明 |

### 5.2 必须建立的数据库约束

1. `project_events(aggregate_id, aggregate_version)` 唯一，防止并发写入两个同版本事件。
2. `commands(project_id, command_type, idempotency_key)` 唯一，保证幂等。
3. 已锁定 DatasetManifest 的内容表禁止 UPDATE/DELETE；新版本通过新主键写入。
4. TaskRun 的状态迁移由状态机检查约束，终态不能回到 RUNNING。
5. Artifact 的 `(checksum, size, artifact_type)` 可建立去重索引，但去重不得破坏项目权限隔离。
6. EvidenceItem 必须引用至少一个 Artifact 和一个通过可接受 QC 的 TaskRun。
7. Claim 的 `claim_level` 不得高于所有支持 EvidenceItem 中允许的最高能力；数据库检查不足的部分由领域验证器强制执行。
8. 每个最终 Claim 必须有 scope；不允许空物种、空组织等无边界泛化。
9. ApprovalDecision 的 subject version 必须存在，且不能批准已 supersede 的旧草案作为当前版本。
10. 所有外键删除默认 `RESTRICT`；历史审计对象不级联删除。

### 5.3 追溯主链

```text
OriginalRequest
  └─ ResearchSpec[v]
      ├─ ScopeBundle[v]
      └─ SubQuestion[v]
          └─ EvidencePlan[v]
              └─ ResourceCandidate
                  └─ DatasetProfile[v]
                      └─ FeasibilityReport
                          └─ DatasetManifest[v, LOCKED]
                              └─ CompatibilityDecision
                                  └─ MethodContract[v]
                                      └─ WorkflowPlan[v]
                                          └─ TaskPacket
                                              └─ TaskRun
                                                  └─ ArtifactManifest
                                                      └─ QCReport
                                                          └─ EvidenceItem
                                                              └─ Claim[v]
                                                                  └─ AlignmentReport
                                                                      └─ Report/Bundle
```

任一链路缺失时：

- 缺少上游引用的对象不得进入正式状态；
- 允许保留失败和草案记录，但必须标记未准入；
- 报告生成时必须输出追溯完整性检查结果。

### 5.4 Schema 文件最小清单

```text
schemas/
├─ project/
│  ├─ original_request.schema.json
│  ├─ project_policy.schema.json
│  ├─ approval_request.schema.json
│  └─ approval_decision.schema.json
├─ research/
│  ├─ research_spec.schema.json
│  ├─ ambiguity_report.schema.json
│  ├─ scope_bundle.schema.json
│  ├─ ontology_mapping.schema.json
│  ├─ subquestion.schema.json
│  ├─ question_dependency_graph.schema.json
│  ├─ evidence_plan.schema.json
│  └─ evidence_gap.schema.json
├─ resources/
│  ├─ resource_candidate.schema.json
│  ├─ search_audit_log.schema.json
│  ├─ dataset_profile.schema.json
│  ├─ paper_profile.schema.json
│  └─ annotation_source_profile.schema.json
├─ data/
│  ├─ dataset_feasibility_report.schema.json
│  ├─ dataset_manifest.schema.json
│  └─ sample_manifest.schema.json
├─ methods/
│  ├─ method_contract.schema.json
│  ├─ compatibility_decision.schema.json
│  └─ method_plan.schema.json
├─ workflow/
│  ├─ workflow_plan.schema.json
│  ├─ task_packet.schema.json
│  └─ artifact_expectation.schema.json
├─ execution/
│  ├─ task_run.schema.json
│  ├─ task_attempt.schema.json
│  └─ tool_call_record.schema.json
├─ quality/
│  ├─ qc_report.schema.json
│  └─ qc_finding.schema.json
├─ evidence/
│  ├─ evidence_item.schema.json
│  ├─ claim.schema.json
│  └─ question_alignment_report.schema.json
└─ reproduction/
   ├─ report_manifest.schema.json
   ├─ reproduction_bundle_manifest.schema.json
   └─ comparison_spec.schema.json
```

---

## 6. 项目状态机、命令和门禁

### 6.1 状态推进原则

- API、CLI、Agent 和 Worker 均不能直接设置 `project.state`。
- 每次推进都由命令处理器执行：读取当前聚合版本 → 校验 guard → 写领域事件 → 更新投影 → 写 outbox。
- 失败、回退和重新规划也必须产生事件；不能删除失败历史。
- 项目状态、子问题状态、TaskRun 状态分别建模，避免用一个超大枚举表达所有层次。

### 6.2 主状态转换表

| 当前状态 | 命令 | 必须满足的 Guard | 事件 | 下一状态 |
|---|---|---|---|---|
| 无 | CreateProject | 原始请求非空；策略可解析 | ProjectCreated、OriginalRequestRecorded | INTAKE |
| INTAKE | NormalizeQuestion | 请求在支持范围；无阻断策略问题 | ResearchSpecDrafted | QUESTION_NORMALIZED |
| QUESTION_NORMALIZED | ResolveScope | ResearchSpec Schema 合格 | ScopeResolved | SCOPE_RESOLVED |
| SCOPE_RESOLVED | DefineSubQuestions | 关键实体有映射或显式未决 | SubQuestionsDefined | SUBQUESTIONS_DEFINED |
| SUBQUESTIONS_DEFINED | PlanEvidence | 每个子问题有成功/失败标准 | EvidencePlansCreated | EVIDENCE_PLANNED |
| EVIDENCE_PLANNED | DiscoverResources | 网络策略允许；工具可用 | ResourcesDiscovered | RESOURCES_DISCOVERED |
| RESOURCES_DISCOVERED | VerifyResources | 候选有外部标识或本地来源 | ResourcesVerified | RESOURCES_VERIFIED |
| RESOURCES_VERIFIED | AssessFeasibility | Profile 信息满足评估最低字段 | FeasibilityReviewed | DATASETS_FEASIBILITY_REVIEWED |
| DATASETS_FEASIBILITY_REVIEWED | LockDatasets | 至少一条证据路径可行；审批满足 | DatasetManifestLocked | DATASETS_LOCKED |
| DATASETS_LOCKED | SelectMethods | Manifest 已锁定；合同注册可用 | MethodsSelected | METHODS_SELECTED |
| METHODS_SELECTED | CompileWorkflow | 每个任务有兼容方法或批准工程任务 | WorkflowCompiled | WORKFLOW_COMPILED |
| WORKFLOW_COMPILED | AuthorizeExecution | DAG 校验通过；环境/资源/审批通过 | ExecutionAuthorized | EXECUTION_AUTHORIZED |
| EXECUTION_AUTHORIZED | StartTasks | 可运行节点存在 | TasksStarted | TASKS_RUNNING |
| TASKS_RUNNING | RegisterArtifacts | 所需 TaskRun 已终止；输出可检查 | ArtifactsRegistered | ARTIFACTS_REGISTERED |
| ARTIFACTS_REGISTERED | RunQC | Artifact 完整性通过 | QCCompleted | QC_COMPLETED |
| QC_COMPLETED | ExtractEvidence | 至少一个结果满足准入或有明确无证据结论 | EvidenceExtracted | EVIDENCE_EXTRACTED |
| EVIDENCE_EXTRACTED | SynthesizeClaims | Evidence 范围和等级可计算 | ClaimsSynthesized | CLAIMS_SYNTHESIZED |
| CLAIMS_SYNTHESIZED | AuditAlignment | ResearchSpec 和 Claim 版本冻结 | AlignmentAudited | ALIGNMENT_AUDITED |
| ALIGNMENT_AUDITED | BuildReport | 无 blocking issue；或明确部分完成 | ReportGenerated | REPORT_READY |
| REPORT_READY | BuildReproductionBundle | 所有必需 Artifact 可导出 | ReproductionBundleBuilt | REPRODUCTION_BUNDLE_READY |
| REPRODUCTION_BUNDLE_READY | CompleteProject | 验收检查通过 | ProjectCompleted | COMPLETED/其他合法终态 |

### 6.3 合法终态及触发条件

| 终态 | 触发条件 | 必须输出 |
|---|---|---|
| COMPLETED | 所有必需验收项通过 | 完整报告、复现包、覆盖矩阵 |
| PARTIALLY_COMPLETED | 部分子问题完成，限制已披露 | 已回答/未回答清单 |
| INSUFFICIENT_DATA | 可用数据无法满足最低证据标准 | Feasibility/EvidenceGap/停止理由 |
| METHOD_NOT_APPLICABLE | 所有允许方法均不兼容 | CompatibilityDecision 集合 |
| INCONCLUSIVE | 结果不稳定或证据不足以形成目标 Claim | QC、冲突和不确定性 |
| CONFLICTING_EVIDENCE | 独立证据方向冲突且无法解释 | 支持/反对证据和综合说明 |
| FAILED_TECHNICALLY | 技术失败超过恢复策略 | 错误链、日志、重跑建议 |
| CANCELLED_BY_USER | 用户明确取消 | 取消对象、时间和保留策略 |
| REQUIRES_HUMAN_DECISION | 关键决策无法安全默认 | 决策包和可选路径 |

### 6.4 人工门禁

| Gate | 触发点 | 审核对象 | 默认自动化等级下是否强制 |
|---|---|---|---|
| G-Q | ResearchSpec/Scope 完成后 | 问题、范围、假设、Claim ceiling | 是 |
| G-D | DatasetManifest 锁定前 | 数据集、样本、排除、许可证、checksum | 是 |
| G-M | 高风险方法或 EngineeringTask | MethodContract、写入范围、测试、风险 | 条件强制 |
| G-E | 执行前 | DAG、环境、资源、网络、输出目录 | 策略自动/异常人工 |
| G-C | 正式 Claim 前 | 等级、支持/反对证据、限制 | 建议强制 |
| G-X | 导出敏感复现包前 | 敏感扫描与接收方策略 | 条件强制 |

### 6.5 重新规划与局部回退

| 发现的问题 | 回退目标 | 不允许的处理 |
|---|---|---|
| ResearchSpec 含义改变 | QUESTION_NORMALIZED 或 SCOPE_RESOLVED | 仅修改报告文字 |
| 数据集被撤回/文件变化 | RESOURCES_VERIFIED / DATASETS_FEASIBILITY_REVIEWED | 静默替换文件 |
| Manifest 样本变化 | 新建 Manifest 版本并回到 DATASETS_LOCKED | 原地改 TSV |
| MethodContract 不兼容 | METHODS_SELECTED | 强行运行后再解释 |
| DAG 节点失败且输入未变 | 局部重跑失败子图 | 全量重跑所有下载 |
| QC 表明设计不成立 | REPLAN 或合法终态 | 调参数直到显著 |
| Claim 越级 | CLAIMS_SYNTHESIZED | 在报告中保留越级句子 |
| Alignment 发现范围漂移 | 对应最早漂移阶段 | 只加免责声明 |

---

## 7. API、CLI、事件和工具接口

### 7.1 API 设计规则

- 读接口与命令接口分离；命令返回 `command_id`、accepted version 和后续查询位置。
- 所有修改命令要求 `Idempotency-Key` 和 `If-Match/expected_version`。
- 长任务返回异步操作资源，不阻塞 HTTP 连接。
- API 只暴露稳定领域对象，不直接泄露内部 ORM 模型。
- 所有列表接口支持 project scope、分页、状态和时间过滤。

### 7.2 最小 API 面

| 类别 | 接口示例 | 结果 |
|---|---|---|
| 项目 | `POST /v1/projects` | 创建 Project、保存 OriginalRequest |
| 项目查询 | `GET /v1/projects/{id}` | 当前投影、状态、阻断项 |
| 时间线 | `GET /v1/projects/{id}/events` | 可审计事件 |
| 命令 | `POST /v1/projects/{id}/commands/{command}` | 提交规范化、检索、编译、执行等命令 |
| 审批 | `POST /v1/approvals/{id}/decisions` | 绑定版本的决定 |
| ResearchSpec | `GET /v1/projects/{id}/research-specs` | 版本列表与确认状态 |
| 资源 | `GET /v1/projects/{id}/resources` | 候选与核验状态 |
| 数据清单 | `GET /v1/projects/{id}/dataset-manifests` | 锁定版本、样本和 checksum |
| 方法 | `GET /v1/method-contracts` | 注册合同和版本 |
| 工作流 | `GET /v1/projects/{id}/workflow-plans/{version}` | DAG、节点、门禁 |
| 任务 | `GET /v1/projects/{id}/tasks` | Packet 和 Run 状态 |
| Artifact | `GET /v1/projects/{id}/artifacts` | 类型、URI、checksum、lineage |
| QC | `GET /v1/projects/{id}/qc-reports` | 四层 QC 与 finding |
| Evidence | `GET /v1/projects/{id}/evidence-items` | 正反证据 |
| Claim | `GET /v1/projects/{id}/claims` | 等级、scope 和审核状态 |
| 报告 | `POST /v1/projects/{id}/commands/build-report` | 报告异步操作 |
| 复现包 | `POST /v1/projects/{id}/commands/build-reproduction-bundle` | bundle 操作 |
| 比较 | `POST /v1/reproduction-comparisons` | 复现比较结果 |

### 7.3 CLI 最小命令

```text
bioauto project create
bioauto project show
bioauto project timeline
bioauto spec normalize
bioauto spec approve
bioauto resources discover
bioauto resources verify
bioauto datasets assess
bioauto datasets lock
bioauto methods select
bioauto workflow compile
bioauto workflow validate
bioauto execution authorize
bioauto execution run
bioauto tasks list
bioauto artifacts verify
bioauto qc run
bioauto evidence extract
bioauto claims synthesize
bioauto alignment audit
bioauto report build
bioauto reproduction build
bioauto reproduction compare
```

CLI 调用同一应用服务或 API，不得复制一套独立业务逻辑。

### 7.4 Tool Broker 合同

每个工具必须声明：

```yaml
tool_id:
version:
capability:
input_schema:
output_schema:
network_domains:
auth_scope:
timeout:
retry_policy:
rate_limit:
data_sensitivity_ceiling:
side_effect_class: READ_ONLY | WRITE_EXTERNAL | EXECUTE
cache_policy:
audit_policy:
```

首批工具建议：

- `resolve_ontology_term`
- `search_public_dataset`
- `fetch_dataset_metadata`
- `list_dataset_files`
- `download_external_file`
- `calculate_checksum`
- `search_publication`
- `fetch_publication_metadata`
- `lookup_annotation_source`
- `inspect_table_schema`
- `inspect_expression_matrix`
- `run_registered_method`
- `run_workflow_engine`
- `inspect_artifact`
- `compare_reproduction_outputs`

### 7.5 外部适配器规则

- 适配器负责外部协议、分页、限流、重试和原始响应留档。
- 领域层只接收标准化结果和核验状态。
- 联网集成测试与离线录制 fixture 分离。
- 外部服务不可用时，系统必须输出可判定的失败类型，不得由 LLM 猜测返回值。
- API 变更只修改适配器和契约测试，不扩散到领域模型。

### 7.6 与完整平台的集成契约

核心系统向并列平台提供：

- 项目状态、任务状态、Artifact、Evidence、Claim、审批、失败升级、审计日志查询；
- 复现包导出和复现比较命令；
- `ApprovalRequested`、`ProjectBlocked`、`TaskFailedFinal`、`ReportReady`、`ReproductionBundleReady` 等领域事件。

核心系统从并列平台接收：

- 用户身份、项目成员与角色；
- 项目创建和自然语言问题；
- ProjectPolicy、数据访问约束和计算凭据引用；
- 绑定版本的批准/驳回；
- 复现提交的 Artifact/Bundle 引用；
- 通知端点引用，而不是把通知服务写入核心领域。

接口规则：

1. 核心闭环可通过 API/CLI 独立运行，聊天界面和完整工作台不是必需依赖；
2. 平台只能发送命令或审批，不能直接改数据库状态；
3. 计算凭据和通知渠道只保存引用，不进入事件 payload；
4. 学生分配、期限和评分留在实验室管理系统，核心系统只处理复现包和比较结果。

---

## 8. Agent、Worker 与审核单元的运行合同

### 8.1 Agent 统一准入流程

```text
构建最小上下文
→ 选择版本化 Prompt
→ 通过 LLM Gateway 调用
→ 解析结构化输出
→ JSON Schema 校验
→ 领域语义校验
→ Policy 校验
→ 保存为 DRAFT
→ 必要时请求人工批准
→ 由命令处理器转为正式对象
```

任何一步失败时不得把自然语言输出直接写入正式对象。

### 8.2 Agent 角色合同

| 角色 | 输入 | 结构化输出 | 确定性后校验 | 禁止行为 |
|---|---|---|---|---|
| Question Normalizer | OriginalRequest、Policy | ResearchSpec、AmbiguityReport | 必填字段、范围冲突、Claim ceiling | 编造数据集或方法 |
| Scope Resolver | ResearchSpec、ontology tool results | ScopeBundle、Mapping | 标准 ID 存在性、置信度阈值 | 把低置信度当确定事实 |
| Evidence Planner | ResearchSpec、SubQuestion schema | SubQuestion、EvidencePlan | 单一关系、覆盖、无环、成功失败标准 | 先选现有方法再改问题 |
| Resource Ranker | 已核验 Candidate/Profile | 排序与理由 | 外部 ID、元数据完整性 | 生成未核验资源 |
| Method Planner | Manifest、MethodContract | MethodPlan、Compatibility proposal | 合同硬条件规则 | 放宽 donor/统计要求 |
| Result Auditor | QC、Artifact 摘要、MethodContract | 审核建议 | 引用存在、级别约束 | 修改结果文件 |
| Evidence Synthesizer | EvidenceItem 集合 | Claim 草案 | Claim ceiling、范围、反对证据 | 读取未准入结果 |
| Alignment Auditor | OriginalRequest、ResearchSpec、Claim | AlignmentReport | 覆盖矩阵和规则检查 | 创建新 Evidence |
| Report Writer | 已批准 Claim/Evidence | 章节结构和叙述 | 每句结论引用、禁用新 Claim | 自由提高证据等级 |

### 8.3 LLM Gateway 必须记录

- provider 和 model 标识；
- prompt template ID、版本和 hash；
- 输入对象 ID/版本，不必在日志中复制敏感全文；
- 工具调用列表；
- 输出原文 Artifact 和结构化对象；
- token/成本或本地资源统计；
- 开始结束时间、错误、重试；
- 安全/数据策略决定；
- 评测器或人工审核结果。

### 8.4 TaskPacket 四种子类型

#### AnalysisTaskPacket

- 只能引用已注册 MethodContract；
- 参数必须通过合同校验；
- 声明统计单位、随机种子、预期 Artifact 和 Claim 能力；
- Worker 不允许动态安装未声明依赖。

#### DataPreparationTaskPacket

- 声明来源、下载策略、checksum、转换步骤和数据敏感级别；
- 不得把预处理结果直接解释为科学证据。

#### ReviewTaskPacket

- 输入只读；
- 输出 QCReport、审计意见或 ApprovalRequest；
- 不得修改被审核对象。

#### EngineeringTaskPacket

- 声明代码仓库、基线 commit、允许写入路径、禁止路径、测试命令和 patch 输出；
- 在隔离分支或工作树执行；
- 只能返回 patch、changed files、测试结果和风险；
- 不得自行合并、部署、修改主分支、放宽 MethodContract 或写最终 Claim。

### 8.5 Worker 状态

```text
CREATED
→ QUEUED
→ CLAIMED
→ RUNNING
→ SUCCEEDED
```

异常分支：

```text
RUNNING → FAILED_RETRYABLE → QUEUED
RUNNING → FAILED_FINAL
RUNNING → TIMED_OUT
QUEUED/RUNNING → CANCELLED
CLAIMED/RUNNING → LEASE_EXPIRED → QUEUED
```

### 8.6 Worker 完成条件

Worker 返回成功前必须：

1. 退出码符合合同；
2. 预期输出全部存在；
3. 文件非空且格式可解析；
4. 为所有输出计算 checksum；
5. 日志和资源用量已登记；
6. 未写入允许范围之外；
7. 输出清单与声明一致；
8. 生成 TaskRun 完成事件；
9. Artifact 尚未通过完整性验证时只标记 `PENDING_VALIDATION`。

---

## 9. 测试与验收体系

### 9.1 测试层次

| 层次 | 验证对象 | 必须覆盖 |
|---|---|---|
| Schema contract | JSON/类型模型 | 正例、缺字段、未知字段、错误 enum、版本兼容 |
| Domain unit | 领域规则 | Claim ceiling、锁定对象、状态 guard、兼容性 |
| State transition | 命令/事件 | 合法/非法转换、并发、重放、幂等 |
| Persistence integration | DB/迁移/outbox | 事务、唯一约束、恢复、投影重建 |
| Adapter contract | 外部资源/对象存储/队列 | 错误映射、重试、录制 fixture |
| Worker integration | Sandbox/Runner | 超时、取消、资源限制、重复消息 |
| Workflow golden | 注册方法和 Nextflow | 固定输入、预期输出、checksum/容差 |
| QC adversarial | 四层 QC | 伪重复、donor 缺失、批次、模型失败、越级 |
| Evidence/Claim | 科学边界 | 正反证据、等级上限、scope、冲突 |
| End-to-end | 整体闭环 | 真实公共数据、合法非成功终态、复现包 |

### 9.2 必备测试夹具

```text
fixtures/
├─ valid_public_bulk/
├─ valid_public_scrna_with_donor/
├─ missing_donor_scrna/
├─ insufficient_replicates/
├─ metadata_group_conflict/
├─ checksum_mismatch/
├─ corrupted_matrix/
├─ transient_download_failure/
├─ permanent_access_denied/
├─ worker_duplicate_message/
├─ worker_timeout/
├─ method_incompatible/
├─ statistical_rank_deficient/
├─ outlier_driven_result/
├─ rna_to_secretion_overclaim/
├─ scope_drift_species/
├─ scope_drift_tissue/
├─ conflicting_evidence/
└─ sensitive_export_violation/
```

### 9.3 Definition of Ready

叶子任务只有在以下条件满足后才可开始：

- 上游对象和版本明确；
- 写入范围明确；
- 接口或 Schema 已冻结；
- 验收命令可执行；
- 需要的 fixture 已存在；
- 外部依赖和权限已确认；
- 失败和回滚方式已定义。

### 9.4 Definition of Done

叶子任务完成必须同时满足：

- 代码/Schema/迁移/文档产物存在；
- 正反例测试均通过；
- lint、类型检查和安全扫描达到基线；
- 数据库或 API 变更有兼容说明；
- 实际命令和真实输出被记录；
- 需求—任务—证据矩阵已更新；
- 已知限制未被隐藏；
- 无超出写入范围的修改；
- 回滚或恢复步骤经过至少一次演练或测试。

---
## 10. 分阶段实施路线总览

### 10.1 递增交付顺序

| Release | 可观察能力 | 依赖 | 退出条件 |
|---|---|---|---|
| R0 架构骨架 | 仓库、Schema、事件、API、审计可运行 | WP-00～05 | 可创建项目并重放状态 |
| R1 规划闭环 | 原始问题→ResearchSpec→Scope→SubQuestion→EvidencePlan | WP-06～07 | 规划对象可确认、版本化、追溯 |
| R2 数据闭环 | 真实资源发现→核验→可行性→Manifest 锁定 | WP-08～10 | 至少一个真实公开数据集被锁定 |
| R3 执行脊柱 | MethodContract→DAG→队列→Worker→Artifact | WP-11～15 | 一个通用测试方法可完整执行 |
| R4 bulk 科学闭环 | bulk 分析→四层 QC→Evidence→Claim→报告 | WP-16、18～21 | 真实 bulk 课题端到端完成 |
| R5 sc/snRNA 与复现 | donor-level 路线、跨结果综合、复现包 | WP-17、22 | sc/snRNA 路线和 clean-room 复现通过 |
| R6 生产化基线 | 安全、可观测、失败恢复、总验收 | WP-23～27 | 20 项最低验收和运行手册通过 |

### 10.2 关键路径

```text
WP-00
→ WP-01
→ WP-02
→ WP-03
→ WP-04
→ WP-05
→ WP-06
→ WP-07
→ WP-08
→ WP-09
→ WP-10
→ WP-11
→ WP-12
→ WP-13
→ WP-14
→ WP-15
→ WP-16
→ WP-18
→ WP-19
→ WP-20
→ WP-21
→ WP-22
→ WP-26
→ WP-27
```

可并行项：

- WP-17 可在 WP-15 和 MethodContract 基础完成后与 WP-18～21 部分并行；
- WP-23、WP-24 从 WP-01 开始建立骨架，但在 WP-26 前必须完成；
- WP-25 从 WP-03 开始持续实施，不应拖到末期一次补齐。

---

## 11. 详细工作包

## WP-00：需求冻结、现状审计与差距矩阵

**目标**：在不修改业务代码的前提下确认当前状态、边界、可复用资产和架构决策。

**进入条件**：两份需求基线可读取；用户允许只读检查现有仓库（若存在）。

| Task ID | 具体行动与方法 | 输入/前置条件 | 输出与写入范围 | 验证、失败条件与门禁 |
|---|---|---|---|---|
| T-00-01 | 保存用户原始目标、两份需求文件 hash 和来源；建立 `spec_id` | 原始文档 | `docs/baseline/source_manifest.yaml` | hash 可重算；缺文件则 BLOCKED |
| T-00-02 | 只读列出仓库目录、语言、依赖、服务、工作流和部署文件 | 仓库访问；不得修改 | `docs/audit/repository_inventory.md` | 清单含路径、用途、状态；无仓库则标记 GREENFIELD |
| T-00-03 | 盘点现有 Schema、数据库表、API、队列、Worker、测试和 CI | T-00-02 | `component_inventory.csv` | 每项标记 EXISTING/PARTIAL/MISSING/UNKNOWN |
| T-00-04 | 盘点现有生信方法、脚本、Nextflow/Snakemake、环境和测试数据 | T-00-02 | `method_asset_inventory.md` | 不把未运行脚本标记为可用方法 |
| T-00-05 | 识别需保留、可替换、应废弃、禁止触碰的对象 | T-00-03/04 | `preserve_replace_retire.yaml` | 每项附理由和引用；删除项不在本阶段执行 |
| T-00-06 | 将详细需求逐条分配 Requirement ID | 两份需求文档 | `docs/baseline/requirements_catalog.csv` | 每条有来源章节、优先级、验收方式 |
| T-00-07 | 建立需求→现有能力→差距→工作包映射 | T-00-03、T-00-06 | `gap_matrix.csv` | 不得存在无工作包的 MUST 需求 |
| T-00-08 | 固定 D-01～D-06 决策、默认值和未决项 | 用户决策或默认值 | `architecture_baseline.yaml` | 状态必须为 CONFIRMED 才允许正式改造 |
| T-00-09 | 编写 ADR-001～ADR-010 草案并交叉检查冲突 | 架构基线 | `docs/adr/ADR-*.md` | ADR 之间无相互矛盾；关键未决项显式标记 |
| T-00-10 | 建立需求—任务—证据矩阵初版 | T-00-06/07 | `docs/acceptance/traceability_matrix.csv` | 所有 Requirement ID 至少映射一个 Task ID |

**退出标准**：

- 现状不是凭推测描述；
- `architecture_baseline.yaml` 已明确绿地/改造模式；
- 没有任何正式代码变更；
- 用户确认范围、非目标、首条验收场景和自动化等级。

**回滚**：删除本阶段新增审计文档即可；不得触碰现有运行系统。

---

## WP-01：仓库骨架、开发环境与质量门

**目标**：建立所有后续工作共享的工程骨架，使每次变更可重复安装、测试和审计。

**进入条件**：WP-00 通过；目录结构和兼容策略已确认。

| Task ID | 具体行动与方法 | 输入/前置条件 | 输出与写入范围 | 验证、失败条件与门禁 |
|---|---|---|---|---|
| T-01-01 | 创建或调整模块化目录；保留现有兼容入口 | ADR-001 | 仓库目录、空包、README | 导入检查通过；不得删除保留模块 |
| T-01-02 | 固定包管理与 lockfile；声明开发、测试、运行依赖分组 | 基线语言 | `pyproject.toml`/lockfile | 空环境可安装；依赖可重现 |
| T-01-03 | 建立统一配置模型，区分环境变量、非敏感配置和 secret reference | ADR-009 | `config/`、settings 模型 | 缺必需配置时快速失败；密钥不入库 |
| T-01-04 | 建立本地 Compose：API、Worker、Postgres、Broker、对象存储 | 部署默认 | `deploy/compose/` | 一条命令启动并通过 healthcheck |
| T-01-05 | 建立数据库迁移框架和空基线迁移 | DB 可用 | `migrations/` | upgrade/downgrade 在临时库通过 |
| T-01-06 | 建立日志格式：时间、level、service、project、correlation、task_run | 配置模型 | logging 配置 | 敏感字段过滤测试通过 |
| T-01-07 | 建立 lint、format、类型检查、单元测试和覆盖率命令 | 包管理完成 | 工具配置、Makefile/Taskfile | 本地与 CI 命令一致 |
| T-01-08 | 建立 CI 基础流水线：安装、lint、type、unit、migration | 仓库托管可用 | CI 配置 | 干净分支可运行；失败阻止合并 |
| T-01-09 | 建立测试目录和 fixture 生命周期规则 | 测试框架 | `tests/` 骨架 | 示例测试在 DB/对象存储隔离运行 |
| T-01-10 | 建立容器镜像构建并固定基础镜像 digest 策略 | 容器运行时 | Dockerfile | 构建无密钥；镜像可启动 |
| T-01-11 | 建立许可证、第三方依赖清单和 SBOM 生成入口 | 依赖锁定 | `docs/compliance/`、CI artifact | 能列出直接/传递依赖 |
| T-01-12 | 建立 PR/变更模板，强制填写 Requirement ID、Task ID、测试与回滚 | 开发流程 | `.github/` 或等效目录 | 模板字段完整 |

**退出标准**：新环境可从零启动；API/Worker healthcheck 成功；CI 基础门禁通过；没有业务逻辑。

---

## WP-02：核心 Schema、枚举与语义验证框架

**目标**：先固定机器可读契约，再实现 Agent、API 和数据库逻辑。

**进入条件**：WP-01 完成；ADR-006 已确认。

| Task ID | 具体行动与方法 | 输入/前置条件 | 输出与写入范围 | 验证、失败条件与门禁 |
|---|---|---|---|---|
| T-02-01 | 实现公共 ID、版本、Actor、时间、Hash、ExternalIdentifier 类型 | 横切契约 | `domain/common/` | 序列化稳定；非法时间/ID 被拒绝 |
| T-02-02 | 实现 Project、OriginalRequest、ProjectPolicy、Approval Schema | 需求字段 | project schemas/models | 正反例与 JSON Schema 快照通过 |
| T-02-03 | 实现 ResearchSpec、AmbiguityReport、ScopeBundle、OntologyMapping | 需求字段 | research schemas/models | 关键研究对象和 open_questions 校验 |
| T-02-04 | 实现 SubQuestion、DependencyGraph、EvidencePlan、EvidenceGap | 需求字段 | research planning schemas | 子问题单一性规则、图无环测试 |
| T-02-05 | 实现 ResourceCandidate、SearchRun、Dataset/Paper/Annotation Profile | 资源需求 | resource schemas | 外部 ID 未核验不能标 VERIFIED |
| T-02-06 | 实现 FeasibilityReport、DatasetManifest、SampleManifest | 数据治理需求 | data schemas | 锁定状态、排除理由、checksum 规则 |
| T-02-07 | 实现 MethodContract、CompatibilityDecision、MethodPlan | 方法需求 | method schemas | forbidden conditions 与 claim capability 必填 |
| T-02-08 | 实现 WorkflowPlan、TaskPacket 子类型、ArtifactExpectation | DAG 需求 | workflow schemas | packet discriminator、读写范围和依赖规则 |
| T-02-09 | 实现 TaskRun、Attempt、ToolCall、ResourceUsage | 执行需求 | execution schemas | 状态与时间顺序、exit code 规则 |
| T-02-10 | 实现 ArtifactManifest、ArtifactEdge | Artifact 需求 | artifact schemas | URI/checksum/producer/lineage 必填 |
| T-02-11 | 实现 QCReport、QCFinding、四层 QC 枚举 | QC 需求 | quality schemas | decision 与 finding 严重度一致 |
| T-02-12 | 实现 EvidenceItem、Claim、AlignmentReport | 证据需求 | evidence schemas | supports/opposes、scope、claim level 规则 |
| T-02-13 | 实现 ReportManifest、BundleManifest、ComparisonSpec | 报告需求 | reproduction schemas | 比较等级和容差条件合法 |
| T-02-14 | 为每个 Schema 生成 JSON Schema、最小正例、边界正例和反例 | T-02-01～13 | `schemas/**/examples/` | CI 自动验证所有样例 |
| T-02-15 | 建立 Schema 兼容性检查器和变更分类 | 所有 schema | `scripts/check_schema_compatibility` | breaking change 必须失败并要求新 major version |

**退出标准**：核心对象可独立序列化、反序列化和导出 Schema；无业务服务也能运行全量契约测试。

---

## WP-03：事件日志、投影、事务和幂等骨架

**目标**：使项目历史可重建，任何状态变化均可追溯且不会因重复投递产生重复副作用。

**进入条件**：WP-02 公共对象完成；PostgreSQL 可用。

| Task ID | 具体行动与方法 | 输入/前置条件 | 输出与写入范围 | 验证、失败条件与门禁 |
|---|---|---|---|---|
| T-03-01 | 设计 aggregate、event、command、outbox、projection 基础表 | ADR-002/004 | 首批 DB migration | 约束、索引、外键评审通过 |
| T-03-02 | 实现 EventStore append，要求 expected aggregate version | T-03-01 | DB repository | 并发双写仅一个成功 |
| T-03-03 | 实现 CommandReceipt 和 idempotency lookup | T-03-01 | command repository | 同 key 重放返回同结果 |
| T-03-04 | 实现同事务写事件、投影和 outbox | T-03-01/02 | unit-of-work | 故障注入下无半提交 |
| T-03-05 | 实现 outbox dispatcher、重试和投递标记 | Broker 可用 | messaging adapter | 重复投递不重复业务写入 |
| T-03-06 | 实现 Project 聚合重放和当前投影生成 | Project events | domain/application | 清空投影后可重建同一状态 |
| T-03-07 | 实现事件 Schema 版本注册与 upcaster 接口 | schema versioning | event registry | 旧 fixture 可重放 |
| T-03-08 | 实现 correlation/causation 传播 | API/Worker context | middleware | 一次闭环可按 correlation 查询 |
| T-03-09 | 实现不可变审计读取接口 | EventStore | query service | 无 UPDATE/DELETE API |
| T-03-10 | 实现数据库备份、恢复和投影重建演练脚本 | 开发 DB | runbook/scripts | 在临时实例恢复并比对事件数/hash |
| T-03-11 | 建立性能基线：事件追加、项目时间线和投影查询 | 基础实现 | benchmark report | 超出约定阈值则记录优化项，不静默跳过 |
| T-03-12 | 建立 event tamper 检查链或批次 hash | EventStore | audit verifier | 修改历史测试能被检测 |

**退出标准**：创建示例项目、追加事件、重复命令、并发命令、清空投影重建和备份恢复全部通过。

---

## WP-04：项目、审批、状态机 API 与 CLI

**目标**：建立可操作的控制面，不依赖聊天界面即可推进和审计项目。

**进入条件**：WP-03 完成；项目与审批 Schema 可用。

| Task ID | 具体行动与方法 | 输入/前置条件 | 输出与写入范围 | 验证、失败条件与门禁 |
|---|---|---|---|---|
| T-04-01 | 实现 CreateProject 命令，保存 OriginalRequest 和初始 Policy | Project schema | command handler/API | 原文 byte/hash 可核验，不被规范化覆盖 |
| T-04-02 | 实现项目查询、列表、当前阻断项和时间线 | 投影/EventStore | query API | 权限与分页测试通过 |
| T-04-03 | 实现主状态枚举、transition registry 和 guard 接口 | 状态表 | domain/state_machine | 所有非法转换有明确 error code |
| T-04-04 | 为 20 个主状态注册命令、事件和目标状态骨架 | T-04-03 | transition definitions | 表驱动测试覆盖每个转换 |
| T-04-05 | 实现 ApprovalRequest 创建、过期、取消和决定 | approval schema | application/API | 决定绑定准确对象版本 |
| T-04-06 | 实现自动化等级 A0～A3 的 gate policy evaluator | ProjectPolicy | policy module | 同一动作在四等级下结果可预测 |
| T-04-07 | 实现命令 API 的 idempotency 和 optimistic concurrency headers | T-03 | HTTP middleware | 重复/陈旧版本返回规范错误 |
| T-04-08 | 实现异步 operation resource 和轮询接口 | outbox/queue | operation API | 命令接受与任务完成状态区分 |
| T-04-09 | 实现 CLI 的 project、timeline、approval 基础命令 | API client | `apps/cli` | CLI 与 API 结果一致 |
| T-04-10 | 实现取消项目与取消未开始任务的命令 | 状态机 | command handler | 已完成历史不删除；取消有事件 |
| T-04-11 | 建立 OpenAPI、错误码和示例请求自动生成 | API | docs artifact | CI 校验文档与实现一致 |
| T-04-12 | 建立权限占位：researcher、reviewer、operator、admin | Identity adapter | auth policy | reviewer 不能伪装为执行者；最小权限测试 |

**退出标准**：仅通过 API/CLI 可创建项目、读取时间线、申请/决定审批并验证非法状态转换。

---

## WP-05：Agent Gateway、Prompt 注册和结构化输出准入

**目标**：将 LLM 变成可替换、可审计、不能越权写事实的推理适配器。

**进入条件**：WP-02、WP-03、WP-04 完成；联网和数据策略已决定。

| Task ID | 具体行动与方法 | 输入/前置条件 | 输出与写入范围 | 验证、失败条件与门禁 |
|---|---|---|---|---|
| T-05-01 | 定义 LLM provider 接口、request/response 和 usage 模型 | ADR-007 | `infrastructure/llm` | fake provider 契约测试通过 |
| T-05-02 | 建立 PromptRegistry：ID、版本、模板 hash、目标 Schema | prompts 目录 | registry/service | 运行时只能使用已注册版本 |
| T-05-03 | 实现结构化输出解析、Schema 校验和有限修复重试 | Schema registry | gateway pipeline | 非法输出永不进入业务表 |
| T-05-04 | 实现领域语义 validator hook | 各对象 validator | admission pipeline | JSON 合法但语义错误被拒绝 |
| T-05-05 | 实现敏感内容分类、最小上下文构建和字段脱敏 | ProjectPolicy | context builder | SENSITIVE 数据外发测试被阻止 |
| T-05-06 | 实现工具调用 allowlist 和 Tool Broker 接口 | tool contracts | tool mediation | Agent 不能调用未授权工具 |
| T-05-07 | 保存原始模型输出为受限 Artifact，业务对象只保存解析结果引用 | Artifact 接口占位 | audit record | 能追溯但不在普通日志泄露全文 |
| T-05-08 | 记录 provider/model/prompt/input version/tool/usage/timing | ToolCallRecord | audit | 每次调用可按 project/correlation 查询 |
| T-05-09 | 实现超时、速率限制、成本预算和断路器 | Policy | gateway reliability | 超预算转 NEED_HUMAN_REVIEW |
| T-05-10 | 建立离线 fake model 和固定回复 fixture | test framework | tests/fakes | CI 不依赖外部模型 |
| T-05-11 | 建立 Agent 评测框架：字段覆盖、歧义暴露、幻觉资源 ID、Claim 越级 | prompt fixtures | evaluation suite | 基线未通过不得升级 Prompt |
| T-05-12 | 实现 Prompt 变更审批和回滚 | registry/version | approval integration | 新 prompt 失败可切回旧版本 |

**退出标准**：fake provider 能产出草案；非法结构、越权工具、敏感数据和超预算均被门禁阻止；所有调用可审计。

---

## WP-06：阶段 0～2——受理、问题规范化与本体范围

**目标**：从原始自然语言生成可审核 ResearchSpec 和 ScopeBundle，不掩盖歧义。

**进入条件**：R0 完成；Question Normalizer 与 Scope Resolver Prompt 已注册。

| Task ID | 具体行动与方法 | 输入/前置条件 | 输出与写入范围 | 验证、失败条件与门禁 |
|---|---|---|---|---|
| T-06-01 | 实现请求支持范围分类器，优先确定性规则，LLM 仅作建议 | OriginalRequest | intake service | 超出范围能进入合法停止状态 |
| T-06-02 | 实现多研究问题检测和拆分建议，不自动拆项目 | OriginalRequest | IntakeAssessment | 混合问题 fixture 被识别 |
| T-06-03 | 实现 ProjectPolicy 初始构建：网络、敏感度、资源、自动化等级 | 用户约束 | policy version | 缺关键敏感策略时请求审批 |
| T-06-04 | 实现 Question Normalizer command 和 Agent 调用 | OriginalRequest/Policy | ResearchSpec DRAFT | 原文与规范化文本均保留 |
| T-06-05 | 实现 ResearchSpec 语义校验：比较组、endpoint、claim ceiling、冲突 | ResearchSpec | ValidationReport | 空比较关系或自相矛盾被阻止 |
| T-06-06 | 实现 AmbiguityReport：事实、假设、默认值、待决策分离 | Agent output | report/version | assumption 不得写入 known facts |
| T-06-07 | 定义 OntologyAdapter 接口和首个公共/本地词表适配器 | 网络策略 | adapter | 查询返回原词、标准 ID、来源、版本 |
| T-06-08 | 实现候选映射排序和置信度，不把单一模型分数当事实 | ontology results | OntologyMapping[] | 低置信度形成 ScopeWarning |
| T-06-09 | 实现物种、疾病、组织、细胞、表型、年龄/时间的跨字段一致性规则 | ScopeBundle | semantic validators | 人/鼠混用、组织/细胞层级冲突被标记 |
| T-06-10 | 实现 G-Q 审批包：原始问题、规范化结果、假设、范围、非目标 | validated draft | ApprovalRequest | 批准绑定 ResearchSpec/Scope 版本 |
| T-06-11 | 实现批准后冻结与 `QUESTION_NORMALIZED`、`SCOPE_RESOLVED` 事件 | approval | immutable versions | 新修订必须新版本重新批准 |
| T-06-12 | 建立 adversarial fixture：模糊组织、未给物种、错误因果目标、多个问题 | fixtures | tests | 系统暴露歧义而不是静默补全 |

**退出标准**：用户可看到并批准系统对问题的正式理解；低置信度映射和关键假设不会隐形进入后续检索。

---

## WP-07：阶段 3～4——子问题拆解与证据计划

**目标**：把研究目标拆为可独立判定、可映射证据和可合法失败的子问题。

**进入条件**：G-Q 通过；ResearchSpec/Scope 版本冻结。

| Task ID | 具体行动与方法 | 输入/前置条件 | 输出与写入范围 | 验证、失败条件与门禁 |
|---|---|---|---|---|
| T-07-01 | 定义子问题拆分规则：单一关系、明确范围、证据类型、成功/失败 | ResearchSpec | deterministic rules | 规则文档与 validator |
| T-07-02 | 实现 Evidence Planner Agent 调用生成 SubQuestion 草案 | 冻结 spec/scope | draft objects | 每个对象引用父问题版本 |
| T-07-03 | 实现子问题语义检查：一个主要关系、scope 不超原问题 | drafts | ValidationReport | 复合问题被拒绝或继续拆分 |
| T-07-04 | 构建 QuestionDependencyGraph 并检测环和不可达节点 | SubQuestions | graph object | DAG 测试通过；环有明确错误 |
| T-07-05 | 为每个子问题分配 priority、allowed_claim_level 和 coverage status | spec ceiling | updated draft | 子问题上限不高于主问题 |
| T-07-06 | 生成 EvidencePlan：证据类型、首选/替代数据、最低元数据、复现 | SubQuestion | plan objects | 每个必需字段有明确内容 |
| T-07-07 | 实现 negative evidence strategy 和 conflicting evidence strategy | plan | plan validators | 不允许只定义阳性证据 |
| T-07-08 | 实现 fallback route 和 stop conditions | plan | plan version | 数据不足时有合法出口 |
| T-07-09 | 实现覆盖检查：每个 SubQuestion 至少一个 plan 或“不回答”决定 | graph/plans | CoverageReport | 无计划节点阻止状态推进 |
| T-07-10 | 生成用户审阅表：问题→子问题→证据→Claim 上限 | all drafts | approval artifact | 用户能识别跑题或遗漏 |
| T-07-11 | 持久化确认版本并推进 SUBQUESTIONS_DEFINED/EVIDENCE_PLANNED | approval/policy | events/objects | 版本冻结和时间线可查 |
| T-07-12 | 建立评测集：方法驱动偏差、证据类型不足、忽略负证据、越级 | fixtures | tests | 预设错误被 validator 或审核发现 |

**退出标准**：所有子问题可单独标记 ANSWERED/PARTIAL/NOT_ANSWERED/NOT_ANSWERABLE；不存在没有证据路径或停止条件的必需子问题。

---

## WP-08：阶段 5——真实资源发现与检索审计

**目标**：通过真实工具发现数据、文献和注释资源，任何标识均可核验和回放。

**进入条件**：EvidencePlan 冻结；公共联网策略允许。

| Task ID | 具体行动与方法 | 输入/前置条件 | 输出与写入范围 | 验证、失败条件与门禁 |
|---|---|---|---|---|
| T-08-01 | 定义 DatasetSearchAdapter、LiteratureSearchAdapter、AnnotationSearchAdapter | Tool Broker | adapter protocols | fake adapter 契约测试 |
| T-08-02 | 实现首个 GEO/同等级公共数据搜索适配器 | 公共 API | infrastructure adapter | 分页、限流、超时和错误映射测试 |
| T-08-03 | 实现首个文献检索适配器，返回唯一标识和来源 | 公共文献 API | adapter | 无唯一标识的结果不能 VERIFIED |
| T-08-04 | 实现注释资源目录适配器，记录版本、证据类型和许可 | annotation sources | adapter/profile | 数据库注释不标实验验证 |
| T-08-05 | 从 EvidencePlan 构造可审计查询词和过滤条件 | plan/scope | SearchQuery object | 查询保留原始实体和标准实体 |
| T-08-06 | 运行搜索并保存工具、时间、参数、分页、原始响应摘要和 hash | adapter | SearchRun/Artifact | 可从记录重放同请求 |
| T-08-07 | 标准化 ResourceCandidate 并按 namespace/identifier 去重 | raw results | candidates | 同资源不因查询差异重复建档 |
| T-08-08 | 建立候选排序，但排名只作为优先级，不作为事实核验 | candidates/plan | RankingProposal | 排名理由可追溯到字段 |
| T-08-09 | 实现搜索停止条件：结果稳定、预算上限、重复率、时间或人工决定 | EvidencePlan | SearchStopDecision | 无限制循环被禁止 |
| T-08-10 | 保存空结果、失败结果和被过滤结果 | SearchRun | audit records | 报告可解释为何无候选 |
| T-08-11 | 建立录制 fixture 和离线回放模式 | successful calls | test fixtures | CI 不调用真实 API |
| T-08-12 | 实现域名白名单、速率限制、User-Agent 和凭据引用 | ProjectPolicy | network policy | 未授权域名调用被阻止 |

**退出标准**：至少能对一个真实研究问题生成已审计的候选资源列表；不存在由 LLM 单独生成的 accession、PMID 或 DOI。

---

## WP-09：阶段 6——资源核验与元数据事实化

**目标**：将候选摘要转换为可用于可行性判定的已核验 Dataset/Paper/Annotation Profile。

**进入条件**：WP-08 产生候选；核验工具可访问。

| Task ID | 具体行动与方法 | 输入/前置条件 | 输出与写入范围 | 验证、失败条件与门禁 |
|---|---|---|---|---|
| T-09-01 | 实现 accession/DOI/PMID/资源版本存在性核验 | Candidate | verification record | 不存在或重定向有明确状态 |
| T-09-02 | 获取并保存数据集元数据原始响应 Artifact | verified accession | raw metadata artifact | checksum 和来源 URI 可查 |
| T-09-03 | 解析物种、组织、疾病、平台、样本、分组、年龄、性别、批次、干预 | raw metadata | DatasetProfile draft | 每字段带来源位置和置信度 |
| T-09-04 | 提取 donor/subject、生物学重复和样本—文件映射 | metadata/files | profile fields | donor 缺失显式 unknown，不推测 |
| T-09-05 | 列出原始、计数、处理矩阵文件；记录格式、大小、访问状态 | file API | file inventory | 文件可见性与实际可下载性分开 |
| T-09-06 | 核验论文—数据集对应关系 | Dataset/Paper candidates | relation record | 仅标题相似不得建立确定关系 |
| T-09-07 | 核验许可证、使用限制、受控访问和再分发条件 | source metadata | policy fields | 不明确时标 NEED_MORE_INFORMATION |
| T-09-08 | 对文献标注证据类型：表达关联、蛋白检测、功能、机制等 | paper metadata/abstract | PaperProfile | 不从摘要夸大机制等级 |
| T-09-09 | 对 annotation source 记录版本、更新时间、实体范围和证据层级 | source | AnnotationSourceProfile | 版本缺失则限制复现等级 |
| T-09-10 | 实现 Profile completeness score 和缺失字段清单 | profiles | completeness report | 分数不替代硬必需字段 |
| T-09-11 | 实现人工修正流程：修正必须保存原值、证据、actor 和新版本 | reviewer input | profile version | 不允许覆盖原始工具事实 |
| T-09-12 | 建立元数据解析黄金样例和格式变更报警 | recorded fixtures | contract tests | 外部字段变化导致测试明确失败 |

**退出标准**：候选资源均有 VERIFIED、REJECTED、INCOMPLETE 或 ACCESS_RESTRICTED 状态；DatasetProfile 足以进入可行性评估或明确列出缺口。

---
## WP-10：阶段 7——数据可行性评估与 DatasetManifest 锁定

**目标**：把“资源存在”与“数据能否回答子问题”分开，并冻结正式分析输入。

**进入条件**：DatasetProfile 已核验；EvidencePlan 和 ProjectPolicy 可用。

| Task ID | 具体行动与方法 | 输入/前置条件 | 输出与写入范围 | 验证、失败条件与门禁 |
|---|---|---|---|---|
| T-10-01 | 定义可行性规则注册表，区分通用、生物学设计、bulk、sc/snRNA 和其他数据 | 需求规则 | `policies/feasibility/` | 每条规则有 ID、版本、严重度和依据 |
| T-10-02 | 实现比较组、独立个体、组织匹配、疾病/药物/年龄混杂检查 | DatasetProfile/ResearchSpec | findings | 不满足硬条件不能 ACCEPT |
| T-10-03 | 实现 sc/snRNA donor、pseudobulk、每组 donor、细胞注释和质量字段检查 | sc profile | findings | donor 缺失禁止假装生物学重复 |
| T-10-04 | 实现 bulk 矩阵类型、分组、协变量、平台和 ID 映射检查 | bulk profile | findings | 不兼容矩阵产生条件或拒绝 |
| T-10-05 | 实现文件可访问性、大小、格式和完整性预检 | file inventory | access report | 仅元数据存在不等于数据可用 |
| T-10-06 | 实现 EvidencePlan 最低证据要求与 DatasetProfile 的匹配评分 | plan/profile | suitability matrix | 评分不能覆盖硬失败规则 |
| T-10-07 | 生成 DatasetFeasibilityReport，输出 ACCEPT/CONDITIONAL/REJECT/NEED_MORE_INFORMATION | rules/findings | immutable report | 决策理由和受影响子问题必填 |
| T-10-08 | 对条件可用数据生成必须满足的 preconditions | report | condition list | 条件未满足不能锁定为正式输入 |
| T-10-09 | 构建候选组合评估，避免每个数据集单独可用但整体证据不足 | reports/plans | coverage matrix | 每个 SubQuestion 有数据路径或 gap |
| T-10-10 | 下载/登记被选文件，计算 checksum，保存下载 manifest | selected files | external artifacts | checksum 不匹配立即停止 |
| T-10-11 | 建立 SampleManifest：样本、donor、分组、协变量、排除和理由 | metadata/files | sample manifest draft | 每个排除有规则/人工理由 |
| T-10-12 | 建立 DatasetManifest：数据集、文件、样本、版本、checksum、子问题、限制 | selected inputs | manifest draft | 引用对象版本全部存在 |
| T-10-13 | 生成 G-D 审批包并展示纳入/排除、许可、未知项和证据覆盖 | draft manifest | approval artifact | 用户批准准确版本 |
| T-10-14 | 锁定 Manifest，禁止原地修改；建立 supersede 流程 | approval | locked object/event | UPDATE 测试失败；新版本可创建 |
| T-10-15 | 建立 `INSUFFICIENT_DATA` 与 `NEED_MORE_INFORMATION` 路径 | no viable data | terminal/replan events | 不因无数据继续编译假任务 |

**退出标准**：至少一个 DatasetManifest 已锁定，或项目以可审计的合法终态结束；正式执行输入均有文件 checksum 和样本级清单。

---

## WP-11：阶段 8——MethodContract 注册、兼容性与方法计划

**目标**：用显式科学合同控制方法适用范围，禁止“有代码就运行”。

**进入条件**：DatasetManifest 锁定；至少一个子问题有可用数据路径。

| Task ID | 具体行动与方法 | 输入/前置条件 | 输出与写入范围 | 验证、失败条件与门禁 |
|---|---|---|---|---|
| T-11-01 | 建立 MethodRegistry 存储、查询、版本、启停和签名验证 | MethodContract schema | registry/service | 未注册/禁用版本不能被选中 |
| T-11-02 | 定义实现引用：代码 commit、容器 digest、入口、环境、许可证 | registry | implementation reference | `latest` 标签不能作为正式版本 |
| T-11-03 | 实现合同硬条件 evaluator：输入类型、元数据、样本设计、协变量、禁止条件 | contract/manifest | deterministic evaluator | 同输入产生稳定结果 |
| T-11-04 | 实现统计单位与 Claim capability 的兼容规则 | subquestion/contract | compatibility findings | cell-level 伪重复不能通过 |
| T-11-05 | 实现必需 QC、预期输出和验证测试的合同校验 | contract | registry admission | 合同不完整不能 ACTIVE |
| T-11-06 | 注册 `bulk_deg` 首版合同 | 方法负责人确认 | method YAML/JSON | 固定小数据黄金测试通过 |
| T-11-07 | 注册 `scrna_pseudobulk_deg` 合同 | donor-level 规则 | method contract | 无 donor fixture 必须不兼容 |
| T-11-08 | 注册 `gene_set_score` 合同 | 评分输入规则 | method contract | claim capability 不允许单独证明衰老/机制 |
| T-11-09 | 注册 `enrichment` 合同 | gene universe/多重检验规则 | method contract | 缺背景集合时有 warning/拒绝策略 |
| T-11-10 | 注册 `differential_abundance` 合同 | donor/样本设计规则 | method contract | 统计单位和最小设计受控 |
| T-11-11 | 注册 `surface_secretome_annotation` 合同 | 注释版本/证据类型 | method contract | 注释不被标为分泌实验证据 |
| T-11-12 | 注册 `cross_dataset_concordance` 合同 | 独立性、ID 映射、效应方向 | method contract | 同数据重复分析不算复现 |
| T-11-13 | 生成每个子问题×Manifest×Method 的 CompatibilityDecision | registry/evaluator | immutable decisions | 硬失败不能被 Agent 覆盖 |
| T-11-14 | Agent 只在兼容集合中排序主方法、备选方法和理由 | decisions | MethodPlan draft | 排名不改变 compatibility |
| T-11-15 | 实现方法风险、替代路线、人工 G-M 和 METHOD_NOT_APPLICABLE 路径 | plan/policy | approval/replan events | 无兼容方法时停止而非自由写代码 |

**退出标准**：每个将执行的分析节点都绑定 ACTIVE MethodContract 版本和 PASS/CONDITIONAL CompatibilityDecision；合同硬条件可自动验证。

---

## WP-12：阶段 9——WorkflowPlan 和 Artifact DAG 编译器

**目标**：把方法计划编译为显式、可静态检查、可局部重跑的任务与 Artifact DAG。

**进入条件**：MethodPlan 完成；DatasetManifest、MethodContract 版本冻结。

| Task ID | 具体行动与方法 | 输入/前置条件 | 输出与写入范围 | 验证、失败条件与门禁 |
|---|---|---|---|---|
| T-12-01 | 定义内部 DAG 图模型、节点/边、artifact port 和 compiler version | workflow schema | compiler core | 图模型单元测试 |
| T-12-02 | 实现 DataPreparation 节点生成：下载、解压、格式转换、ID 映射 | manifest/contracts | task drafts | 每个输出有 ArtifactExpectation |
| T-12-03 | 实现 Analysis 节点生成：method、参数、环境、资源和 claim capability | MethodPlan | task drafts | 参数通过合同校验 |
| T-12-04 | 实现 Review/QC 节点生成，不与分析节点合并 | required_qc | review tasks | 计划、执行、审核职责分离 |
| T-12-05 | 实现 Artifact ID 级输入输出绑定，不使用路径猜依赖 | expectations | edges | 改路径不改变 DAG 语义 |
| T-12-06 | 实现拓扑排序、环检测、孤立节点和缺生产者检查 | graph | compiler diagnostics | 预设环必须编译失败 |
| T-12-07 | 实现子问题覆盖检查：每个必需 SubQuestion 至少一条终止证据路径 | graph/plans | coverage report | 无路径阻止编译 |
| T-12-08 | 实现 allowed_claim_level 沿任务和证据路径传播 | contracts/subquestions | node annotations | 下游不能自行提高上限 |
| T-12-09 | 实现资源估计、timeout、retry policy、network policy、write scope 注入 | policy/contracts | complete packets | 缺关键执行字段阻止编译 |
| T-12-10 | 实现条件分支和 fallback，但分支条件必须结构化 | evidence plan | conditional DAG | 不允许在脚本中隐藏主要分支 |
| T-12-11 | 实现受影响子图计算，用于 Manifest/参数/方法变更后的局部重跑 | graph lineage | invalidation service | 修改图参数只标记正确后代 |
| T-12-12 | 生成可读 DAG Markdown/Graphviz 和机器 JSON | WorkflowPlan | artifacts/docs | 可视化与机器节点数一致 |
| T-12-13 | 生成 `TaskPacket[]` 与 `ArtifactExpectation[]` 不可变版本 | compiler output | persisted plan | content hash 可重算 |
| T-12-14 | 建立 compiler golden tests 和确定性测试 | fixed inputs | tests | 同输入/版本产生同 plan hash |
| T-12-15 | 编译失败映射为可操作诊断或 REPLAN，不生成半成品正式计划 | errors | failure handling | 半成品只能 DRAFT |

**退出标准**：至少一个真实项目编译出无环 DAG；所有输入有来源、所有输出有消费者或终端用途、所有子问题有证据路径或明确 gap。

---

## WP-13：阶段 10——执行授权、队列投递与任务调度

**目标**：在代码实际运行前完成输入、环境、资源、网络、目录和审批检查，并可靠投递任务。

**进入条件**：WorkflowPlan 编译并冻结；队列和 Worker 骨架可用。

| Task ID | 具体行动与方法 | 输入/前置条件 | 输出与写入范围 | 验证、失败条件与门禁 |
|---|---|---|---|---|
| T-13-01 | 实现 preflight checker 框架和结构化检查结果 | WorkflowPlan | preflight report | 每项有 PASS/WARN/FAIL |
| T-13-02 | 检查输入 Artifact 存在、状态 VALID、checksum 匹配 | artifact registry | findings | checksum mismatch 阻止执行 |
| T-13-03 | 检查环境引用可解析、容器 digest 存在、代码 commit 可访问 | method implementation | findings | `latest` 或缺 digest 失败 |
| T-13-04 | 检查资源配额、磁盘空间、超时和并发限制 | policy/worker status | findings | 资源不足进入 WAITING/REPLAN |
| T-13-05 | 检查网络白名单、凭据引用、数据敏感度和输出存储策略 | ProjectPolicy | findings | 敏感数据外发被阻止 |
| T-13-06 | 检查输出目录隔离、只读输入和 write_scope | TaskPackets | findings | 路径逃逸 fixture 被拒绝 |
| T-13-07 | 检查随机种子、方法参数和 Manifest/Contract/Plan 版本冻结 | plan | findings | 缺种子或动态参数按合同处理 |
| T-13-08 | 计算需要的 G-E/G-M 审批并创建请求 | policy/preflight | ApprovalRequest | 未批准不得投递 |
| T-13-09 | 实现 ExecutionAuthorized 事件和授权快照 | all pass | immutable snapshot | 快照含所有对象版本/hash |
| T-13-10 | 实现 ready-node scheduler：仅投递依赖满足节点 | DAG/projections | scheduler | 未完成依赖节点不入队 |
| T-13-11 | 使用 outbox 投递任务消息，消息只含 TaskPacket ID/版本和 trace context | T-03 | queue messages | DB 提交失败不投递 |
| T-13-12 | 实现任务优先级、项目公平性和并发上限基础策略 | ProjectPolicy | scheduler policy | 单项目不能饿死其他项目 |
| T-13-13 | 实现取消、暂停、恢复和不再调度后继节点 | command/state | control paths | 已运行任务按可取消能力处理 |
| T-13-14 | 建立调度故障注入：重复消息、Broker 断连、进程崩溃 | test infra | resilience tests | 状态不丢失且无重复副作用 |

**退出标准**：通过 preflight 的 DAG 能按依赖逐节点投递；未授权、输入损坏、网络违规或资源不足的任务不会执行。

---

## WP-14：阶段 11——Worker、Sandbox、执行器与 TaskRun

**目标**：在受限环境中可靠执行四类 TaskPacket，并形成完整、可恢复的运行记录。

**进入条件**：WP-13 可投递任务；对象存储接口可用。

| Task ID | 具体行动与方法 | 输入/前置条件 | 输出与写入范围 | 验证、失败条件与门禁 |
|---|---|---|---|---|
| T-14-01 | 实现 Worker 消息消费、幂等检查和 TaskPacket 版本读取 | queue/task repo | worker core | 重复消息不创建重复有效 Run |
| T-14-02 | 实现 task lease、heartbeat、过期回收和 worker identity | DB/queue | lease service | worker 崩溃后任务可安全接管 |
| T-14-03 | 实现 Sandbox 目录：inputs 只读、work 临时、outputs 限定 | filesystem/object store | sandbox module | 路径逃逸和软链接攻击测试 |
| T-14-04 | 实现 LocalProcessExecutor，捕获 stdout/stderr/exit code/signal | sandbox | executor | 超时和非零退出映射正确 |
| T-14-05 | 实现 ContainerExecutor，固定 digest、只读根文件系统和资源限制 | container runtime | executor adapter | 未声明 mount/network 被拒绝 |
| T-14-06 | 实现 NextflowExecutor adapter，传入 profile、workdir、params 和 run ID | Nextflow | workflow executor | 能捕获 run metadata 和 trace |
| T-14-07 | 为后续 HPC 定义 SlurmExecutor 接口，不在 MVP 实现全部调度细节 | ADR | interface/docs | 不让 HPC 假设渗入领域层 |
| T-14-08 | 实现 TaskRun/Attempt 生命周期、时间、参数、环境和代码版本记录 | execution schema | run repository | 状态顺序与终态约束测试 |
| T-14-09 | 采集 CPU、内存、磁盘、wall time 和重试次数 | executor | resource usage | 采集失败不丢业务结果但产生 warning |
| T-14-10 | 实现 timeout、graceful cancel、hard kill 和清理 | policy | runner | 无孤儿进程/容器 |
| T-14-11 | 实现 retry 分类器，只对 retryable 错误重试 | error envelope | retry logic | 样本不足/不兼容绝不自动重试 |
| T-14-12 | 实现输出枚举，拒绝未声明写入和超出配额文件 | ArtifactExpectation | output scanner | undeclared output 被登记警告或拒绝 |
| T-14-13 | 将日志和原始运行元数据作为受控 Artifact 上传 | object store | log artifacts | 敏感字段过滤通过 |
| T-14-14 | 实现 EngineeringTaskPacket 的独立工作树、patch 导出和禁止 merge | git repo | engineering executor | 主分支 hash 执行前后不变 |
| T-14-15 | 建立 crash、timeout、OOM、cancel、duplicate、lease expiry 测试 | test fixtures | integration tests | 每种故障得到预期 TaskRun 状态 |

**退出标准**：四种 packet 至少有 fake/smoke 执行；通用分析任务可成功、重试、取消和失败；运行记录足以复现执行过程。

---

## WP-15：阶段 12——Artifact 登记、完整性和 Lineage

**目标**：让所有输入、中间产物和输出成为受验证、可追溯的正式对象。

**进入条件**：TaskRun 能生成输出；对象存储已接入。

| Task ID | 具体行动与方法 | 输入/前置条件 | 输出与写入范围 | 验证、失败条件与门禁 |
|---|---|---|---|---|
| T-15-01 | 实现 ObjectStore 接口：put/get/head/list/presign/delete-policy | ADR-003 | storage adapter | 本地与 S3-compatible 契约测试 |
| T-15-02 | 实现流式 checksum 和大小计算，避免大文件读入内存 | files | checksum service | 固定 fixture hash 正确 |
| T-15-03 | 实现 Artifact 注册状态：PENDING_VALIDATION→VALID/INVALID/QUARANTINED | output files | registry | 未验证 Artifact 不能被正式消费 |
| T-15-04 | 校验预期输出是否全部出现、非空、文件名/类型符合声明 | expectations | integrity findings | 必需输出缺失使 TaskRun 不可准入 |
| T-15-05 | 实现格式检测和 Schema 校验：TSV/CSV/JSON/矩阵/图像元数据 | artifacts | validators | 后缀与内容不一致被发现 |
| T-15-06 | 实现未经声明文件扫描和 write_scope 违规处理 | sandbox outputs | findings | 违规文件隔离并阻断 |
| T-15-07 | 生成 ArtifactManifest：URI、checksum、size、format、producer、sensitivity | validated file | immutable record | 所有字段可追溯 |
| T-15-08 | 生成 ArtifactEdge：input/derived-from/produced-by/used-by | TaskPacket/Run | lineage graph | 图无悬空边 |
| T-15-09 | 实现图表→源表→任务→输入的 lineage 约束 | report artifacts | lineage check | 最终图无源表则 blocking |
| T-15-10 | 实现去重但保留项目级权限和逻辑引用 | checksum index | storage optimization | 不跨权限泄露存在性 |
| T-15-11 | 实现 retention、tombstone、legal hold 和证据关联保护 | policy | lifecycle service | 正式证据文件硬删除被拒绝 |
| T-15-12 | 实现 Artifact 查询、下载授权和 checksum 复核 API/CLI | registry | interfaces | 下载后 hash 一致 |
| T-15-13 | 实现 lineage 导出 JSON/CSV/Graphviz | graph | export artifact | 节点/边数与数据库一致 |
| T-15-14 | 建立损坏、空文件、格式错、未声明文件、重复文件测试 | fixtures | tests | 预期决策全部匹配 |

**退出标准**：任一执行输出在进入 QC 前都有有效 ArtifactManifest 和完整 lineage；损坏或越权文件不会进入正式结果池。

---

## WP-16：首条真实 bulk RNA-seq 执行路线

**目标**：先用较简单但真实的分析链验证通用执行、Artifact、QC、Evidence 架构。

**进入条件**：WP-10～15 完成；`bulk_deg` 等合同 ACTIVE；选择公开验收数据集。

| Task ID | 具体行动与方法 | 输入/前置条件 | 输出与写入范围 | 验证、失败条件与门禁 |
|---|---|---|---|---|
| T-16-01 | 固定 bulk 验收课题、数据集、预期比较和 gold acceptance，不将其硬编码 | G-D approval | acceptance fixture docs | 课题可更换且代码不变 |
| T-16-02 | 实现 bulk 文件 adapter：counts/expression、sample metadata、gene ID | DatasetManifest | preparation module | schema/维度/样本一致性检查 |
| T-16-03 | 实现基因 ID 标准化和映射报告，保留未映射与多重映射 | source version | mapping artifacts | 映射损失率和规则可见 |
| T-16-04 | 实现 sample design matrix builder 和共线/满秩预检 | sample manifest | design artifact | rank-deficient fixture 在执行前失败 |
| T-16-05 | 实现 `bulk_deg` runner 或 Nextflow module，参数完全来自 MethodContract | active contract | method implementation | 小型黄金数据结果在容差内 |
| T-16-06 | 输出标准化 DEG 表：effect、SE/CI、p、adjusted p、base expression、flags | method result | result schema/artifact | 列、类型、排序独立校验 |
| T-16-07 | 实现基础图形生成，图只引用标准结果表 | result artifact | plot artifacts | lineage 指向源表 |
| T-16-08 | 实现 gene-set score 或 sample-level pathway score 路线 | contract | result artifacts | 评分不能单独生成机制 Claim |
| T-16-09 | 实现 enrichment 路线，记录 universe、方向、多重检验和数据库版本 | contract | enrichment artifacts | 缺 universe 按合同 warning/失败 |
| T-16-10 | 实现 surface/secretome annotation join，保留来源和证据级别 | annotation profile | annotated candidate table | annotation 不等于实验支持 |
| T-16-11 | 为每个模块提供固定输入、预期输出和参数边界测试 | method testdata | tests | 容器内外结果可比较 |
| T-16-12 | 运行真实公开数据并登记 TaskRun、Artifact 和运行摘要 | full pipeline | execution records | 无手工移动未登记文件 |
| T-16-13 | 建立负向路线：样本不足、分组冲突、矩阵不兼容 | invalid fixtures | tests | 正确终止而非强行运行 |
| T-16-14 | 记录性能和资源基线，为后续配额提供事实 | real run | benchmark artifact | 不把一次运行外推为保证 |

**退出标准**：真实 bulk 数据从锁定 Manifest 执行到标准化 Artifact；科学结论尚须通过 WP-18～21，不允许在本工作包提前宣布完成。

---

## WP-17：sc/snRNA donor-level 路线与跨数据集一致性

**目标**：实现第一版单细胞/单核纵向路线，并强制 donor-level 科学边界。

**进入条件**：通用执行脊柱可用；公开 sc/snRNA 数据有 donor 与分组信息；合同 ACTIVE。

| Task ID | 具体行动与方法 | 输入/前置条件 | 输出与写入范围 | 验证、失败条件与门禁 |
|---|---|---|---|---|
| T-17-01 | 固定 sc/snRNA 验收课题与公开数据，不硬编码疾病或组织 | approval | fixture docs | 代码仅依赖 Manifest/Scope |
| T-17-02 | 实现 AnnData/等效矩阵 adapter，核验 obs/var、稀疏矩阵和层 | DatasetManifest | data adapter | 矩阵维度和 metadata 一致 |
| T-17-03 | 实现 donor、sample、group、batch、cell type 字段映射与来源记录 | metadata | mapping artifact | donor unknown 不自动推断 |
| T-17-04 | 实现细胞层基础 QC 统计和排除标记，不覆盖原始矩阵 | raw data | QC/prepared artifacts | 排除规则版本化 |
| T-17-05 | 实现 cell annotation 接受已有注释或受控重建接口 | profile/contract | annotation artifact | 原注释与新注释并存可追溯 |
| T-17-06 | 实现 donor×cell_type pseudobulk 聚合 | prepared data | pseudobulk artifacts | 聚合后单位数与 donor 映射核验 |
| T-17-07 | 实现低细胞数 donor/cell type 处理规则 | contract | inclusion report | 规则不在运行中临时改变 |
| T-17-08 | 运行 donor-level pseudobulk DEG | method contract | DEG artifacts | 与 cell-level 伪重复 fixture 区分 |
| T-17-09 | 实现 differential abundance 路线，明确统计单位和模型 | contract | DA artifacts | donor 数不足时不运行 |
| T-17-10 | 实现 cell-type gene-set score 汇总，区分细胞值与 donor 统计 | contract | score artifacts | 不把细胞数当独立重复 |
| T-17-11 | 实现候选细胞类型特异性指标和限制 | endpoints | candidate table | 明确相对/绝对特异性定义 |
| T-17-12 | 接入 surface/secretome annotation 和文献支持，不提升证据层级 | annotations | enriched candidates | RNA 与蛋白/分泌分开 |
| T-17-13 | 实现跨数据集 gene ID/cell type 映射、效应方向和独立性检查 | multiple manifests | concordance artifacts | 同 cohort 重处理不算独立复现 |
| T-17-14 | 输出一致、冲突、不可比较和缺失结果 | concordance | evidence-ready tables | 不只保留一致阳性结果 |
| T-17-15 | 建立缺 donor、细胞数不平衡、批次混杂、注释冲突的 adversarial tests | fixtures | tests | 各错误触发预期 gate |
| T-17-16 | 运行真实公开 sc/snRNA 路线并记录资源/限制 | full pipeline | TaskRuns/Artifacts | 失败也必须保留完整审计 |

**退出标准**：至少一个 donor-level 路线真实运行；缺 donor 数据被明确拒绝或降级；跨数据集综合保留冲突与不可比较结果。

---

## WP-18：阶段 13——四层 QC 引擎与门禁

**目标**：把执行成功与科学可用分开，按规则版本生成可操作的 QC 决策。

**进入条件**：Artifact 已登记；MethodContract 声明 required_qc。

| Task ID | 具体行动与方法 | 输入/前置条件 | 输出与写入范围 | 验证、失败条件与门禁 |
|---|---|---|---|---|
| T-18-01 | 建立 QC Rule Registry：rule ID、层级、版本、适用对象、严重度、动作 | requirements/methods | `policies/qc/` | 规则可查询和冻结版本 |
| T-18-02 | 实现 QC 执行框架，输入只读并输出 QCFinding/QCReport | Artifact/TaskRun | QC engine | 审核单元不能修改结果 |
| T-18-03 | 实现 Execution QC：退出、日志、环境、参数、资源、预期输出 | TaskRun | execution report | warning/error 解析有规则 |
| T-18-04 | 实现 Data QC 通用：身份、分组、缺失、异常、批次、ID 映射 | data artifacts | data report | 样本与矩阵错位被发现 |
| T-18-05 | 实现 sc/snRNA Data QC：低质量、doublet/ambient 指标、donor/metadata、注释 | sc artifacts | report | 工具能力不足时显式 NOT_ASSESSED |
| T-18-06 | 实现 Statistical QC：单位、伪重复、满秩、共线、收敛、多重检验 | result/design | report | 伪重复 fixture REJECT/REPLAN |
| T-18-07 | 实现离群和敏感性分析检查接口 | method outputs | findings | 单样本驱动结果产生 warning/block |
| T-18-08 | 实现 Biological QC：层级匹配、RNA/蛋白、marker 特异性、富集解释、跨物种 | evidence candidates | biological report | 预设越级被阻止 |
| T-18-09 | 实现 required_qc coverage：合同要求的规则必须全部执行或说明不能执行 | MethodContract | coverage report | 缺必需 QC 不得 PASS |
| T-18-10 | 实现综合决策：PASS/PASS_WITH_WARNINGS/RETRY/REPLAN/REJECT/NEED_HUMAN_REVIEW | findings/policy | QCReport | 决策规则确定且可解释 |
| T-18-11 | 将 findings 绑定具体 Artifact、样本、基因集、TaskRun 或子问题范围 | QC output | scoped findings | 不使用无范围笼统警告 |
| T-18-12 | 实现局部重跑建议：受影响节点、原因、可变参数边界 | lineage/DAG | remediation proposal | 不允许为显著性任意调参 |
| T-18-13 | 实现人工审核覆盖，仅可在政策允许情况下接受 warning，不可抹掉 finding | approval | review record | 原决策和覆盖理由均保留 |
| T-18-14 | 建立四层 QC adversarial 套件并固定预期决策 | fixtures | tests | 每个预设错误至少一个测试 |
| T-18-15 | 生成 QC 摘要和机器可读全量报告 | all reports | artifacts/API | 报告层可读取但不能改写 |

**退出标准**：方法合同要求的 QC 均有结果；只有 PASS 或政策允许的 PASS_WITH_WARNINGS 可进入 Evidence 准入；其他状态进入重试、重规划、拒绝或人工审核。

---
## WP-19：阶段 14——EvidenceItem 准入与证据登记

**目标**：只把通过规定 QC 的观察结果转化为结构化证据，并保留负证据与限制。

**进入条件**：QCReport 完成；至少一个结果可判定是否准入。

| Task ID | 具体行动与方法 | 输入/前置条件 | 输出与写入范围 | 验证、失败条件与门禁 |
|---|---|---|---|---|
| T-19-01 | 定义 Evidence admission policy，映射 QC 决策、证据类型和 Claim 能力 | MethodContract/QC | policy rules | REJECT 结果不能正式准入 |
| T-19-02 | 实现 EvidenceBuilder，输入标准结果表和 QC，禁止读取任意目录 | validated artifacts | builder service | 无 lineage 的结果被拒绝 |
| T-19-03 | 提取 observation、effect size、uncertainty、统计支持和 scope | result schema | EvidenceItem draft | 不只保存 p 值 |
| T-19-04 | 绑定 dataset、task run、artifact、subquestion 和 method contract 版本 | lineage | evidence links | 所有引用存在且可追溯 |
| T-19-05 | 计算 allowed_claim_level，不高于 MethodContract、SubQuestion 和 QC 共同上限 | contracts/QC | field | 上限算法表驱动测试 |
| T-19-06 | 实现 supports/opposes/neutral/inconclusive 分类 | result direction/plan | evidence relation | 非显著不自动等于无效应 |
| T-19-07 | 实现 replication_status，核验 cohort 独立性和数据重用 | manifests | status | 同一 cohort 不标 replicated |
| T-19-08 | 保留 negative、conflicting、failed-to-replicate EvidenceItem | all eligible results | evidence registry | 查询默认不隐藏负结果 |
| T-19-09 | 将未通过 QC 的结果存档为 `NON_ADMISSIBLE_RESULT`，不创建正式 EvidenceItem | rejected QC | archive marker | 报告无法误读为证据 |
| T-19-10 | 实现 EvidenceItem 撤回/失效事件，不物理删除历史 | upstream invalidation | status event | 受影响 Claim 被自动标 stale |
| T-19-11 | 实现 Evidence 查询、scope/filter 和追溯 API | registry | API/CLI | 支持按子问题、方向、QC、数据集查询 |
| T-19-12 | 建立证据准入测试：显著但 QC 失败、数据库注释、单队列、冲突结果 | fixtures | tests | 每类得到正确等级与状态 |

**退出标准**：正式证据池中每项均有完整 lineage、QC 和等级；负证据不会被过滤；未准入结果与正式 Evidence 明确隔离。

---

## WP-20：阶段 15～16——Claim 综合与原问题对照审核

**目标**：形成不超过证据边界的科学表述，并检查是否仍在回答原始问题。

**进入条件**：EvidenceItem 集合稳定；ResearchSpec/SubQuestion 版本冻结。

| Task ID | 具体行动与方法 | 输入/前置条件 | 输出与写入范围 | 验证、失败条件与门禁 |
|---|---|---|---|---|
| T-20-01 | 定义 Claim level ordering 和每级最小证据规则 | requirements/methods | claim policy | 因果等级必须要求适当干预/机制证据 |
| T-20-02 | 定义证据综合维度：方向、效应量、不确定性、独立性、物种、组织、平台、QC | Evidence schema | synthesis rules | 规则版本化 |
| T-20-03 | 实现确定性预聚合，生成支持、反对、不可比较和缺失证据摘要 | EvidenceItems | synthesis input | Agent 不直接扫描原始结果 |
| T-20-04 | Evidence Synthesizer 仅基于结构化摘要生成 Claim 草案 | pre-aggregation | claim draft | 每句 statement 有 evidence IDs |
| T-20-05 | 实现 Claim ceiling validator：主问题、子问题、方法和 Evidence 多重上限 | all caps | validation report | 越级 draft 被拒绝 |
| T-20-06 | 实现 scope intersection，禁止从局部组织/物种外推到普遍结论 | evidence scopes | claim scope | scope 不可空泛 |
| T-20-07 | 强制列出 opposing_evidence、limitations 和 unanswered_questions | evidence set | claim fields | 缺反对证据说明时不准入 |
| T-20-08 | 实现冲突证据处理：降级、并列解释或 `CONFLICTING_EVIDENCE` | conflict rules | decision | 不通过多数票掩盖异质性 |
| T-20-09 | 生成 G-C 审批包：Claim、等级、范围、证据、反证、限制 | validated draft | approval | 批准绑定 Claim version |
| T-20-10 | 实现 Question coverage：每个 SubQuestion 四态判定 | claims/evidence | coverage report | 未回答项不能被报告遗漏 |
| T-20-11 | 实现 Scope fidelity：物种、组织、疾病、年龄、细胞、比较、时间 | spec/claims | alignment findings | 漂移 fixture 被发现 |
| T-20-12 | 实现 Evidence traceability 完整性检查 | claim links | findings | 断链 Claim blocking |
| T-20-13 | 实现 Claim ceiling 审核规则：RNA→分泌、相关→机制、单队列→普遍、注释→实验 | claim text/levels | corrections | 预设越级全部阻断 |
| T-20-14 | 实现 omission audit：负结果、失败分支、限制和未回答项 | project records | findings | 报告前必须处理 blocking issue |
| T-20-15 | 生成 QuestionAlignmentReport、ClaimCorrection、ReportBlockingIssue | all audits | immutable reports | 无 blocking 才可 REPORT_READY |
| T-20-16 | 建立对抗评测：故事化选择、范围漂移、因果词、忽略反证 | fixtures | tests | 所有预设错误被规则或审核捕获 |

**退出标准**：正式 Claim 均有明确等级、scope、支持与反对 Evidence；AlignmentReport 对每个子问题给出状态，且不存在未处理阻断项。

---

## WP-21：阶段 17——受约束报告生成与追溯索引

**目标**：从已审核对象生成报告，不允许报告层创造新证据或提高结论等级。

**进入条件**：AlignmentReport 无未处理 blocking issue，或项目明确进入部分完成/其他合法终态。

| Task ID | 具体行动与方法 | 输入/前置条件 | 输出与写入范围 | 验证、失败条件与门禁 |
|---|---|---|---|---|
| T-21-01 | 定义 ReportInputBundle，只包含批准版本的 Spec、Manifest、QC、Evidence、Claim、Alignment | audited objects | input schema | 未审核对象不能进入 bundle |
| T-21-02 | 定义报告章节模板和机器 JSON Schema | requirements | templates/schemas | 13 项最低内容均有位置 |
| T-21-03 | 实现结构化章节构建器，先生成数据表和引用索引 | input bundle | report intermediate | 所有引用 ID 存在 |
| T-21-04 | Report Writer 仅负责受约束叙述，不允许新增 Claim | structured bundle | prose draft | claim sentence 与 Claim ID 一一对应 |
| T-21-05 | 实现正结果、负结果、冲突、未回答和失败分支的必含规则 | project status | report validation | 缺任一适用章节即失败 |
| T-21-06 | 实现方法、参数、样本排除、QC、限制和后续验证章节 | manifests/runs | sections | 不使用“详见代码”替代关键信息 |
| T-21-07 | 建立图表清单，每个图表引用源表 Artifact 和 TaskRun | artifact lineage | trace index | 无源表图阻止发布 |
| T-21-08 | 输出 Markdown 和机器 JSON；HTML/PDF 作为可选 renderer | report model | report artifacts | 各格式内容摘要一致 |
| T-21-09 | 生成原始问题覆盖矩阵和方法追溯表 | traceability | appendices | Requirement/SubQuestion 可逐项审计 |
| T-21-10 | 实现语言层 claim lint：因果词、普遍化词、蛋白/分泌词与等级冲突 | report text | lint findings | 越级语句阻止正式报告 |
| T-21-11 | 生成 ReportManifest，固定所有输入对象版本和输出 checksum | final artifacts | manifest | 再生成可比较且不静默换输入 |
| T-21-12 | 实现报告审批/发布状态：DRAFT→REVIEWED→RELEASED | approval | status events | DRAFT 不对外标正式结果 |
| T-21-13 | 建立 golden report、缺章节、断链引用和越级语言测试 | fixtures | tests | 预期错误全部阻断 |

**退出标准**：报告可同时供人阅读和机器解析；任何结论、图和表均可点击/查询回上游证据与运行记录。

---

## WP-22：阶段 18——复现包、干净环境重跑与结果比较

**目标**：把项目导出为第三方可执行、可核验和可比较的完整包。

**进入条件**：ReportManifest 完成；所有必需 Artifact 有可导出权限。

| Task ID | 具体行动与方法 | 输入/前置条件 | 输出与写入范围 | 验证、失败条件与门禁 |
|---|---|---|---|---|
| T-22-01 | 实现 BundleBuilder，按固定目录从对象和 Artifact 构建包 | Report/Workflow/Artifacts | bundle staging | 不从人工临时目录收集文件 |
| T-22-02 | 导出 ResearchSpec、Scope、SubQuestions、EvidencePlan、Manifest、WorkflowPlan | versioned objects | JSON/TSV files | schema validation 通过 |
| T-22-03 | 导出 TaskPackets、脚本、工作流、参数、环境定义和代码引用 | execution records | bundle files | commit/digest 不缺失 |
| T-22-04 | 导出 qc_rules、known_limitations、original_run_summary、trace index | QC/audit | docs/json | 负结果和限制保留 |
| T-22-05 | 生成 `checksums.sha256` 并验证 bundle 内每个文件 | bundle | checksum file | 任一不匹配使 bundle INVALID |
| T-22-06 | 生成 `run_order.md` 和可执行入口，明确先决条件、资源和网络 | WorkflowPlan | run guide | 新用户不需推断运行顺序 |
| T-22-07 | 生成 ComparisonSpec：逐 Artifact 定义 bitwise/numeric/direction/set/claim 规则 | method outputs | comparison spec | 每个预期输出有比较策略或理由 |
| T-22-08 | 实现敏感信息、绝对路径、密钥和内部 URI 扫描 | ProjectPolicy | export scan | 检测到阻断信息时 G-X |
| T-22-09 | 生成 ReproductionBundleManifest，记录内容、版本、checksum、许可和接收限制 | bundle | immutable manifest | 与实际文件一致 |
| T-22-10 | 在干净容器/工作目录执行 bundle，不复用原运行 workdir | valid bundle | reproduction TaskRuns | 能从声明输入重建结果 |
| T-22-11 | 实现比较器并输出七级结果 | original/reproduced | comparison report | 容差、方向和候选集合规则可审计 |
| T-22-12 | 将复现失败分类为环境、输入、数值、方法、科学不可比 | comparison | failure analysis | 不简单归为“失败” |
| T-22-13 | 实现 bundle 版本和 supersede，不原地覆盖已交付包 | new input version | new manifest | 旧 bundle 可继续验证 |
| T-22-14 | 建立 clean-room CI/smoke 和 checksum tamper 测试 | fixture bundle | tests | 修改任一文件被发现 |

**退出标准**：第三方可依 README/run_order 在干净环境运行，并得到 ComparisonSpec 定义的可比较结果；敏感信息扫描通过。

---

## WP-23：安全、权限、密钥和数据策略硬化

**目标**：将安全策略落实到命令、工具、Worker、Artifact 和导出链路，而不是只写文档。

**进入条件**：WP-01 已建立配置骨架；在 R6 前必须全部完成。

| Task ID | 具体行动与方法 | 输入/前置条件 | 输出与写入范围 | 验证、失败条件与门禁 |
|---|---|---|---|---|
| T-23-01 | 实现身份适配器和 RBAC/项目成员关系 | auth baseline | auth module | 跨项目访问被拒绝 |
| T-23-02 | 定义 researcher/reviewer/operator/admin 的最小权限矩阵 | roles | policy file | 自我批准、执行者审核自身被阻止 |
| T-23-03 | 实现 secret reference provider，日志/API/TaskPacket 不携带明文密钥 | secret store | infra adapter | secret scanning 零明文 |
| T-23-04 | 实现网络 egress policy：域名、协议、端口和 task type | ProjectPolicy | network guard | 未授权访问 fixture 被阻止 |
| T-23-05 | 实现 Worker 文件系统隔离、非特权用户、只读输入和资源限制 | executor | runtime security | 容器逃逸基础测试/配置审计 |
| T-23-06 | 实现 Artifact sensitivity 继承与降级审批 | lineage/policy | sensitivity evaluator | 高敏输入不能自动产出 PUBLIC |
| T-23-07 | 实现外部 LLM/tool 数据外发决策和字段级脱敏 | gateway/tool broker | policy enforcement | 受限数据不出边界 |
| T-23-08 | 实现审计日志访问控制和防篡改检查 | EventStore | audit security | 普通用户不能修改/读取敏感 payload |
| T-23-09 | 实现依赖、镜像、secret、静态代码和许可证扫描 CI | build | CI gates | 高严重度问题阻断 release |
| T-23-10 | 实现数据保留、归档、tombstone 和合法删除流程 | policy | lifecycle | 有证据/审计关联的对象不能误删 |
| T-23-11 | 实现复现包导出接收方、许可和敏感扫描门禁 | WP-22 | G-X policy | 不合规导出被阻止 |
| T-23-12 | 编写 incident runbook：密钥泄露、错误数据外发、恶意文件、供应链问题 | security design | runbooks | tabletop 演练记录 |
| T-23-13 | 建立安全对抗测试：路径遍历、命令注入、恶意压缩包、未授权 URI | fixtures | tests | 所有阻断并留审计 |

**退出标准**：安全控制有自动测试和运行证据；敏感数据、密钥、网络和导出策略无法由 Agent 或 Worker 绕过。

---

## WP-24：可观测性、审计查询和运行面板基础

**目标**：不依赖登录服务器检查文件即可回答项目、任务、数据、方法、QC 和 Claim 的当前状态。

**进入条件**：基础服务可运行；事件、TaskRun 和日志已有结构。

| Task ID | 具体行动与方法 | 输入/前置条件 | 输出与写入范围 | 验证、失败条件与门禁 |
|---|---|---|---|---|
| T-24-01 | 定义 trace/span/metric/log 命名和 correlation 传播规范 | ADR | observability spec | API→command→queue→worker→tool 同 trace |
| T-24-02 | 接入 API、DB、队列、Worker、外部工具和 Nextflow 的 tracing | telemetry stack | instrumentation | 一次 E2E trace 可查看 |
| T-24-03 | 定义指标：项目状态、队列深度、运行时间、失败率、重试、资源、成本 | requirements | metrics | 标签避免高基数泄露 |
| T-24-04 | 实现结构化日志和集中收集；敏感字段过滤 | logging | collector config | 敏感 fixture 不出现在日志 |
| T-24-05 | 实现 Worker 心跳、lease、最后进度和卡死检测 | execution | health service | 卡死任务产生 alert/event |
| T-24-06 | 实现项目状态查询视图：当前阶段、等待原因、阻断项和下一动作 | projections | API/CLI | 不需要解析日志 |
| T-24-07 | 实现任务视图：依赖、尝试、资源、日志、输出和重试理由 | TaskRun | query | 每次 attempt 可区分 |
| T-24-08 | 实现 QC/Evidence/Claim 变化视图和审批记录 | domain objects | query endpoints | 能回答哪些结果尚待审核 |
| T-24-09 | 实现工具调用和 Agent 调用审计查询 | ToolCallRecord | query | 可按 provider/tool/prompt/version 过滤 |
| T-24-10 | 建立 SLO/报警基线：队列停滞、失败激增、存储异常、DB 连接、预算 | metrics | alerts/runbooks | 报警可关联项目和处理手册 |
| T-24-11 | 实现审计导出，不包含未授权敏感 payload | audit | export | 权限与脱敏测试 |
| T-24-12 | 编写运行手册：常见告警、诊断路径和恢复命令 | observability | runbooks | 新 operator 可按步骤处理 fixture incident |

**退出标准**：对任一项目可明确回答“到哪一步、为什么等待/失败、用了什么、哪些通过 QC、哪些 Claim 待审”。

---

## WP-25：失败分类、重试、重新规划和局部重跑

**目标**：把失败处理变成可预测领域行为，防止无限重试、全量重跑和计划持续偏差。

**进入条件**：事件、DAG、Worker、QC 和 lineage 已可用；从 WP-03 起逐步实现。

| Task ID | 具体行动与方法 | 输入/前置条件 | 输出与写入范围 | 验证、失败条件与门禁 |
|---|---|---|---|---|
| T-25-01 | 建立错误码目录并映射 12 类失败 | error envelope | error catalog | 每个外部/内部异常映射明确 |
| T-25-02 | 定义 retryability matrix：网络、限流、锁、worker 中断等 | failure types | retry policy | 科学/设计失败标 non-retryable |
| T-25-03 | 实现指数退避、jitter、最大次数和总时限 | retryable errors | scheduler | 无无限重试 |
| T-25-04 | 实现 retry budget 按项目/工具/任务类型限制 | policy | budget tracker | 超预算进入人工/失败状态 |
| T-25-05 | 实现 partial failure：独立子图继续，依赖子图阻断 | DAG | scheduler behavior | 不相关路径不被错误取消 |
| T-25-06 | 实现 invalidation 和 affected-subgraph 计算 | lineage/version changes | rerun plan | 只重跑正确后代 |
| T-25-07 | 实现 REPLAN command，记录触发问题、回退阶段和新对象版本 | failure/QC | events | 旧计划保留可审计 |
| T-25-08 | 实现 RECONFIRM_REQUIRED：范围、架构、数据、方法、风险或验收变化 | change detector | approval request | 未确认不执行新路径 |
| T-25-09 | 实现人工 override 边界，只可调整政策允许项，不能伪造 PASS | approvals | override record | 原 finding 不删除 |
| T-25-10 | 实现 terminal decision engine：不足、方法不适用、冲突、技术失败 | project records | terminal event | 终态理由和证据必填 |
| T-25-11 | 建立故障演练：API 中断、Broker 丢失、对象存储暂不可用、Worker 崩溃 | test env | chaos results | 状态可恢复且无重复副作用 |
| T-25-12 | 建立计划持续偏差测试：关键假设失效后后继节点必须停止 | changed fixture | tests | 旧计划不能继续执行 |

**退出标准**：每类失败都有明确的自动重试、人工审核、重新规划、局部重跑或合法终止路径；不会用重复执行掩盖科学不可行。

---

## WP-26：端到端验收、对抗测试与需求覆盖

**目标**：证明系统满足原始需求，而不是只证明模块存在或单元测试通过。

**进入条件**：WP-00～25 的必需项完成；真实验收场景已冻结。

| Task ID | 具体行动与方法 | 输入/前置条件 | 输出与写入范围 | 验证、失败条件与门禁 |
|---|---|---|---|---|
| T-26-01 | 固定 E2E 环境、代码 commit、镜像 digest、Prompt、方法和数据 checksum | release candidate | acceptance baseline | 任一变化需新验收版本 |
| T-26-02 | 执行 planning-only 场景，验证 ResearchSpec/SubQuestion/EvidencePlan | R1 | acceptance report | G-Q 和版本追溯通过 |
| T-26-03 | 执行真实资源发现、核验、可行性与锁定 | R2 | acceptance report | accession/文件/checksum 真实核验 |
| T-26-04 | 执行通用 DAG、队列、Worker、Artifact 和局部重跑场景 | R3 | acceptance report | 幂等、依赖和 lineage 通过 |
| T-26-05 | 执行真实 bulk 完整闭环 | R4 | report/bundle | 正负结果和限制完整 |
| T-26-06 | 执行真实 sc/snRNA donor-level 完整闭环 | R5 | report/bundle | 无 donor 负例被拒绝 |
| T-26-07 | 执行 `INSUFFICIENT_DATA` 场景 | fixture | terminal report | 不生成伪 Claim |
| T-26-08 | 执行 `METHOD_NOT_APPLICABLE` 和 `CONFLICTING_EVIDENCE` 场景 | fixtures | terminal reports | 终态与证据一致 |
| T-26-09 | 注入 scope drift、RNA→分泌、相关→因果、遗漏负结果 | adversarial fixtures | Alignment/QC results | 至少四类预设错误被阻断 |
| T-26-10 | 执行重复消息、Worker crash、checksum mismatch、外部限流 | resilience fixtures | recovery report | 无重复正式 Artifact/Claim |
| T-26-11 | 在干净环境执行复现包并比较 | WP-22 | comparison report | 达到预定一致性等级 |
| T-26-12 | 从最终图/Claim 反向追溯到 OriginalRequest、数据、参数和运行 | trace API | trace audit | 任一断链为 blocking |
| T-26-13 | 更新 20 项最低验收矩阵和所有 Requirement ID 状态 | traceability matrix | acceptance docs | 不允许以“N/A”无理由跳过 MUST |
| T-26-14 | 生成未完成、偏离、限制、风险和后续 backlog | all reports | release gap report | 不隐藏失败项 |
| T-26-15 | 召开最终 G-Release 审核，决定 COMPLETED/PARTIAL/其他终态 | evidence | approval/event | 只能基于验收证据决定 |

**退出标准**：全部最低验收项有可核验证据；端到端真实数据和合法失败场景均通过；复现包 clean-room 运行成功或差异符合 ComparisonSpec。

---

## WP-27：发布、运维移交与后续演进边界

**目标**：将通过验收的版本交给使用者和维护者，并冻结 MVP 边界。

**进入条件**：WP-26 完成；发布决定已批准。

| Task ID | 具体行动与方法 | 输入/前置条件 | 输出与写入范围 | 验证、失败条件与门禁 |
|---|---|---|---|---|
| T-27-01 | 生成 release manifest：代码、Schema、DB、Prompt、Method、镜像、数据 fixture 版本 | release candidate | release manifest | 所有 hash/digest 可核验 |
| T-27-02 | 标记数据库迁移和兼容区间，执行生产前恢复演练 | migrations/backups | migration runbook | 升级失败可恢复 |
| T-27-03 | 生成安装、配置、启动、停止、备份、恢复和升级文档 | deployment | operator docs | 新环境演练通过 |
| T-27-04 | 生成研究用户指南：创建项目、确认、查看阻断、解释终态 | API/CLI | user guide | 非工程人员按指南完成示例 |
| T-27-05 | 生成方法负责人指南：注册合同、验证测试、Claim 能力 | MethodRegistry | method guide | 新方法不能绕过合同流程 |
| T-27-06 | 生成审核人员指南：G-Q/G-D/G-M/G-C/G-X | approval system | reviewer guide | 审批示例绑定准确版本 |
| T-27-07 | 生成事故、失败、数据撤回、方法撤回和 Claim 失效 runbook | system behaviors | runbooks | tabletop 演练完成 |
| T-27-08 | 建立版本与弃用策略：API、Schema、Event、Method、Prompt | ADR-006 | compatibility policy | 破坏性变化有迁移窗口 |
| T-27-09 | 建立受控 backlog，区分核心闭环与完整平台功能 | gap report | roadmap | 非核心需求不挤入本体 |
| T-27-10 | 建立生产数据接入前额外门禁：隐私、伦理、合规、存储和模型外发 | policy | readiness checklist | 未通过不得处理敏感真实数据 |
| T-27-11 | 归档需求—任务—证据矩阵、验收报告和 ADR | all docs | release docs | 可独立审计 |
| T-27-12 | 生成正式完成报告：完成/部分完成/未完成及证据 | acceptance | final report/event | 文件数和测试数不替代语义结论 |

**退出标准**：部署、使用、审核、恢复和扩展均有文档和演练证据；MVP 发布内容与非目标边界清楚。

---
## 12. 原需求 20 项最低验收的落地映射

| AC ID | 原始完成条件 | 主要工作包 | 必须提交的验收证据 | 拒绝条件 |
|---|---|---|---|---|
| AC-01 | 用户可用自然语言提出研究问题 | WP-04、06 | CreateProject 请求、OriginalRequest hash、项目事件 | 原文被覆盖或只能通过内部脚本提交 |
| AC-02 | 产生可验证 ResearchSpec | WP-02、05、06 | Schema、语义报告、G-Q 审批版本 | 仅有自由文本或假设未暴露 |
| AC-03 | 拆成 SubQuestion | WP-07 | SubQuestion 列表、依赖图、单一关系测试 | 复合问题或范围超出主问题 |
| AC-04 | 每个子问题有 EvidencePlan | WP-07 | 覆盖报告、负证据/停止条件 | 必需子问题无计划和停止理由 |
| AC-05 | 通过工具发现并核验真实数据 | WP-08、09 | SearchRun、原始响应、外部 ID 核验 | accession 由 LLM 单独给出 |
| AC-06 | 产生 DatasetFeasibilityReport | WP-10 | 规则版本、findings、决策和理由 | 只按关键词相关性判断可用 |
| AC-07 | 锁定数据集和样本 | WP-10 | DatasetManifest、SampleManifest、checksum、G-D | Manifest 可原地修改或无排除理由 |
| AC-08 | 根据 MethodContract 选择方法 | WP-11 | 合同、CompatibilityDecision、MethodPlan | 有脚本即运行或硬条件被 Agent 覆盖 |
| AC-09 | 编译无环可执行 DAG | WP-12 | WorkflowPlan、编译诊断、拓扑/覆盖测试 | 依赖靠路径猜测或存在缺生产者 |
| AC-10 | 隔离执行并产生完整 TaskRun | WP-13、14 | 授权快照、运行日志、环境、参数、资源 | 无 sandbox、无版本或无退出记录 |
| AC-11 | 所有输出登记为 Artifact | WP-15 | ArtifactManifest、checksum、lineage | 未登记文件直接进入 QC/报告 |
| AC-12 | 完成四层 QC | WP-18 | 四类 QCReport、coverage、decision | 只检查程序退出码 |
| AC-13 | 只有通过门控结果进入 EvidenceItem | WP-19 | admission policy、Evidence links | QC 失败结果成为正式证据 |
| AC-14 | Claim 不超过证据等级 | WP-20 | Claim validator、G-C、越级对抗测试 | RNA 写成分泌或相关写成因果 |
| AC-15 | Alignment 能发现预设跑题/越级 | WP-20、26 | AlignmentReport、adversarial results | 预设错误未阻断正式报告 |
| AC-16 | 输出正、负和未回答项 | WP-19～21 | Evidence 查询、报告章节验证 | 默认隐藏负结果或未回答项 |
| AC-17 | 生成完整复现包 | WP-22 | BundleManifest、checksums、run_order | 手工打包或缺环境/参数 |
| AC-18 | 第三方可重跑并比较 | WP-22、26 | clean-room TaskRun、ComparisonReport | 复用原 workdir 或无比较规则 |
| AC-19 | 图表和结论可追溯到输入、参数和运行 | WP-15、20、21、26 | trace audit、lineage export | 任一断链或用文件名猜来源 |
| AC-20 | 数据不足能合法终止 | WP-10、25、26 | INSUFFICIENT_DATA 事件和报告 | 仍生成伪结果或把失败写成完成 |

---

## 13. 实际执行波次与人工停点

### Wave 0：只读确认

执行：`WP-00`。

人工停点：确认现有仓库、保留对象、D-01～D-06 和非目标。未确认时不得开始正式代码变更。

### Wave 1：控制面骨架

执行：`WP-01 → WP-02 → WP-03 → WP-04 → WP-05`。

可演示结果：

- 本地环境可启动；
- 可创建项目并保存原始请求；
- 事件可重放；
- 审批绑定版本；
- fake Agent 的非法输出被拒绝。

人工停点：审查 Schema、事件和权限边界。若此处错误，后续所有模块都会建立在错误合同上。

### Wave 2：规划闭环

执行：`WP-06 → WP-07`。

可演示结果：自然语言问题形成可确认的 ResearchSpec、Scope、SubQuestion 和 EvidencePlan。

人工停点：G-Q。确认系统没有把示例疾病、已有方法或历史项目预设带入新问题。

### Wave 3：真实数据闭环

执行：`WP-08 → WP-09 → WP-10`。

可演示结果：真实公共资源被发现、核验、评估并锁定。

人工停点：G-D。检查 accession、样本、donor、比较组、许可证、排除、文件和 checksum。

### Wave 4：执行脊柱

执行：`WP-11 → WP-12 → WP-13 → WP-14 → WP-15`。

可演示结果：注册方法被编译为 DAG，在隔离环境执行并登记 Artifact；尚不形成科学 Claim。

人工停点：审查 MethodContract、DAG、write scope、retry 和 Artifact lineage。

### Wave 5：第一条科学纵向链

执行：`WP-16 → WP-18 → WP-19 → WP-20 → WP-21`。

可演示结果：真实 bulk 路线生成经 QC 的 Evidence、受限 Claim 和报告。

人工停点：G-C。重点检查统计单位、负证据、RNA/蛋白边界和 Claim 等级。

### Wave 6：单细胞与复现

执行：`WP-17 → WP-18 增量规则 → WP-19/20 增量 → WP-22`。

可演示结果：donor-level sc/snRNA 路线、跨数据集一致性和 clean-room 复现。

人工停点：确认缺 donor 路线被拒绝，跨队列“复现”没有重复计算同一 cohort。

### Wave 7：硬化与发布

执行：`WP-23 → WP-24 → WP-25 → WP-26 → WP-27`。

可演示结果：安全、监控、失败恢复、20 项验收、发布和运维移交完成。

人工停点：最终 Release Gate，只能依据验收矩阵决定完成状态。

---

## 14. 高风险项与停止条件

| Risk ID | 风险 | 早期信号 | 控制措施 | 强制停止条件 |
|---|---|---|---|---|
| R-01 | Agent 幻觉数据集/论文 ID | 无工具记录却出现 accession/PMID | Tool Broker 核验、Profile 状态机 | 唯一标识未核验不得进入 Feasibility |
| R-02 | 问题被现有方法反向塑形 | 子问题总是匹配已有模块 | EvidencePlan 先于方法、G-Q | ResearchSpec/子问题偏离原意 |
| R-03 | 伪重复或统计单位错误 | cell 数被当重复、donor 缺失 | MethodContract、Statistical QC | 无合法统计单位不得运行正式推断 |
| R-04 | DatasetManifest 静默变化 | 文件/样本与批准版本不一致 | immutable manifest、checksum | 任一正式输入 hash 不匹配 |
| R-05 | 重复消息产生重复运行或 Claim | 同 task 多个有效结果 | idempotency、lease、唯一约束 | 无法判定权威 run 时停止下游 |
| R-06 | Worker 越权写文件或修改主分支 | 未声明输出、仓库 hash 变化 | sandbox、write scope、patch-only | 越权写入立即隔离并安全审计 |
| R-07 | QC 形式化，只查文件存在 | 明显伪重复仍 PASS | 四层规则和对抗 fixture | 必需 QC 未执行不得准入 Evidence |
| R-08 | 报告层创造新结论 | 报告句子无 Claim ID | ReportInputBundle、claim lint | 无引用科学句阻断发布 |
| R-09 | RNA/注释被写成蛋白或分泌验证 | Claim 使用“证实分泌”等词 | Claim ceiling、Biological QC | 越级语句未纠正不得 REPORT_READY |
| R-10 | 复现包依赖原机隐含状态 | 绝对路径、latest 镜像、手工文件 | clean-room、digest、checksums | 干净环境不能启动或关键文件缺失 |
| R-11 | 外部 API 变化破坏解析 | fixture 契约失败、字段缺失 | adapter 隔离、录制 fixture | Profile 硬字段无法可靠提取 |
| R-12 | 敏感数据外发 | 日志、LLM 或导出含受限字段 | sensitivity、脱敏、G-X | 数据策略不明确或扫描阻断 |
| R-13 | 计划持续偏差 | 关键假设失效仍继续后继节点 | RECONFIRM_REQUIRED、invalidation | 影响架构/数据/方法/验收的假设失效 |
| R-14 | 过早微服务化导致系统无法闭环 | 大量服务无纵向功能 | 模块化单体、release 切片 | 新服务不直接服务核心闭环 |
| R-15 | 工程任务放宽科学标准 | patch 修改 MethodContract/QC | 独立 reviewer、禁止路径 | Engineering Worker 触碰禁止对象 |
| R-16 | 只完成阳性 happy path | 无合法失败场景 | WP-25/26 对抗和终态测试 | INSUFFICIENT_DATA 等场景未通过 |

---

## 15. 代码与数据变更控制

### 15.1 必须进入 `RECONFIRM_REQUIRED` 的变化

- 新增或删除主要交付物；
- 改变模块边界、权威状态源或执行引擎；
- 更换首个真实数据课题并影响验收；
- 修改 DatasetManifest、样本纳入或排除；
- 更换关键方法、统计单位、协变量或 Claim ceiling；
- 需要处理敏感/受控数据或外发数据；
- 需要访问真实密钥、生产环境或不可逆删除；
- 修改 20 项最低验收的含义或证据；
- 成本、风险、权限或不可逆性显著上升。

### 15.2 可以作为局部实现细节处理的变化

前提是接口、Schema、科学语义、风险和验收均不变：

- 内部函数拆分或命名；
- 等价的日志格式微调；
- 不改变行为的性能优化；
- 测试代码重构；
- 适配器内部重试实现替换。

所有局部变化仍需有测试和审计，不得借“实现细节”改变业务语义。

---

## 16. Codex 工作包投递模板

每次给 Codex 的请求应基于单个工作包进一步切成小批次，避免一次要求其“实现整个系统”。

```markdown
# Work Order: <Task ID 范围>

## 1. 目标
只完成：……

## 2. 当前事实
- 基线 commit：……
- 已完成依赖：……
- 可使用的 Schema/API：……

## 3. 允许写入
- `path/a/**`
- `path/b/file.py`

## 4. 禁止修改
- 已锁定 Schema：……
- 数据库历史迁移：……
- MethodContract/QC/Claim：……
- 主分支、密钥和生产配置

## 5. 输入
- 文件/对象及版本：……
- fixture：……

## 6. 分步任务
1. 先只读审计相关文件并列出实际状态；
2. 实现 T-xx-01；
3. 运行指定测试；
4. 只有 T-xx-01 通过后实现 T-xx-02；
5. 更新追溯矩阵和变更说明。

## 7. 必须输出
- 修改文件清单；
- diff 摘要；
- 新增/修改的 Schema、迁移或接口；
- 测试命令和真实输出；
- 未完成项和限制；
- 回滚步骤。

## 8. 验收命令
```bash
<exact commands>
```

## 9. 停止条件
- 发现依赖不存在或与文档不符；
- 需要修改禁止范围；
- 需要改变 Schema/架构/方法/验收；
- 测试暴露基线行为冲突。

遇到停止条件时，不要自行扩大范围；返回事实、影响和建议变更请求。
```

### 16.1 推荐的单次 Codex 粒度

- 一次只处理一个 bounded context；
- 一次数据库变更只包含一个可回滚迁移主题；
- 一次 API 工作只实现一组同聚合命令/查询；
- 一次方法工作只实现一个 MethodContract 及其黄金测试；
- 一次 QC 工作只实现一组同层规则；
- 一次工作包必须在同一回复中给出真实测试结果；
- 不把“新增 Schema + 数据库 + API + Worker + UI + E2E”合并为一个请求。

### 16.2 推荐拆分示例

错误：

```text
请实现 WP-10 到 WP-18，完成数据分析系统。
```

正确拆分：

```text
1. 只实现 T-10-01～T-10-04 的可行性规则和单元测试；
2. 审核通过后实现 T-10-05～T-10-09 的报告与覆盖矩阵；
3. 再实现 T-10-10～T-10-14 的下载、Manifest 和锁定；
4. 单独实现 T-11-01～T-11-05 的 MethodRegistry 骨架；
5. 每个初始 MethodContract 分别作为独立 Work Order；
6. DAG 编译、调度、Worker 和 Artifact 各自独立验收。
```

---

## 17. 用户侧项目管理清单

### 17.1 每个工作包开始前

- [ ] 上游工作包退出标准已通过；
- [ ] 当前对象版本和基线 commit 已记录；
- [ ] 写入范围和禁止范围已确认；
- [ ] 叶子任务有可运行验收命令；
- [ ] 需要的 fixture、权限、密钥引用和外部服务可用；
- [ ] 人工门禁是否触发已计算；
- [ ] 追溯矩阵已有对应 Requirement ID。

### 17.2 每个工作包结束后

- [ ] 所有输出文件/对象存在并通过 Schema；
- [ ] 正例、反例、异常和恢复测试均运行；
- [ ] 数据库迁移可升级和恢复；
- [ ] API/事件/Schema 文档同步；
- [ ] 真实命令、输出、日志和 checksum 已保存；
- [ ] 未完成项、已知限制和偏离明确；
- [ ] 回滚或恢复路径可执行；
- [ ] 需求—任务—证据矩阵更新；
- [ ] 下一工作包的进入条件已满足。

### 17.3 每个 Release 结束后

- [ ] 演示的是可观察纵向能力，不是文件数量；
- [ ] 至少一个失败/停止场景通过；
- [ ] 所有关键对象可从事件和数据库重建；
- [ ] Agent 无法绕过工具、Schema、审批和 QC；
- [ ] 新增能力未扩大到非目标；
- [ ] ADR、风险和后续 backlog 已更新。

---

## 18. 最终执行确认卡

### 我对本任务的理解

本次交付是一个**系统架构落地计划**，不是直接实现系统。计划已经把详细需求转化为：

- 架构边界和默认技术路线；
- 权威数据、状态机和审批门禁；
- 核心 Schema、数据库、API、Agent、Worker、QC、Evidence、Claim 和复现架构；
- 28 个工作包与 373 个唯一、可审计叶子任务；
- 递增 Release、验收矩阵、风险、失败出口和 Codex 投递模板。

### 执行前建议冻结

1. 是否存在需改造的现有仓库；
2. 首个真实公开验收课题与数据集；
3. 本地/HPC/云部署目标；
4. 自动化等级 A0～A3；
5. LLM 与数据外发策略；
6. Nextflow 是否作为首个 Workflow Worker。

### 建议确认方式

```text
确认按本计划执行，默认采用 D-01～D-06 建议值。
```

或：

```text
确认，但修改：D-xx = ……；WP-xx 暂不执行；首个验收课题为……。
```

在确认前，允许进行 WP-00 的只读审计；不建议直接进入批量代码生成。

---

## 19. 最终完成判定

只有当以下三项同时成立，才可以把项目标记为 `COMPLETED`：

1. **需求语义完成**：20 项最低验收和所有 MUST Requirement 均有证据，不以代码量、文件数或测试数量替代；
2. **科学边界完成**：QC、Evidence、Claim 和 Alignment 门禁对正例、负例、冲突和越级场景均有效；
3. **可重复运行完成**：第三方可以从锁定数据、代码、环境、参数和复现包重新运行，并按 ComparisonSpec 得到可比较结果。

若任一项不成立，必须选择 `PARTIALLY_COMPLETED`、`INSUFFICIENT_DATA`、`METHOD_NOT_APPLICABLE`、`INCONCLUSIVE`、`CONFLICTING_EVIDENCE`、`FAILED_TECHNICALLY` 或 `REQUIRES_HUMAN_DECISION`，并明确未完成证据。
